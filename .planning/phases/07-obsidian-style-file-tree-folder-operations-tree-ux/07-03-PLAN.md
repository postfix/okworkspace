---
phase: 07-obsidian-style-file-tree-folder-operations-tree-ux
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - web/src/components/__regression__/treeBehaviors.test.tsx
  - web/src/components/LeftTree.tsx
  - web/src/components/LeftTree.css
  - web/src/components/TreeContextMenu.tsx
  - web/src/components/TreeContextMenu.css
  - web/src/components/RenameModal.tsx
  - web/src/components/MoveDialog.tsx
  - web/src/components/LeftTree.test.tsx
autonomous: true
requirements: [TREE-01]
must_haves:
  truths:
    - "Every shipped tree behavior is pinned by a regression test BEFORE any rebuild edit"
    - "After the rebuild, no shipped tree behavior regresses (page menu, page DnD, folder-scoped create, dialog footer, commit-wait update)"
    - "The page right-click context menu still offers Rename/Move/Version history/Delete (editor) and Version history only (reader)"
    - "RenameModal and MoveDialog are parameterized by node kind (page now; folder wiring lands in 07-04) with no page-behavior regression"
    - "The rebuilt components import the 07-01/07-02 client functions without throwing (mock factory updated)"
  artifacts:
    - path: "web/src/components/__regression__/treeBehaviors.test.tsx"
      provides: "Clean-Rebuild Behavior Inventory regression net (pinned before rebuild)"
      contains: "regression"
    - path: "web/src/components/LeftTree.tsx"
      provides: "rebuilt tree preserving all shipped behaviors (no new features yet)"
      contains: "application/x-okf-page"
    - path: "web/src/components/TreeContextMenu.tsx"
      provides: "rebuilt a11y-preserving context menu (items-array contract)"
      contains: "role=\"menu\""
  key_links:
    - from: "web/src/components/LeftTree.test.tsx"
      to: "../api/client mock"
      via: "vi.mock factory updated with renameFolder/moveFolder/deleteFolder/restoreFolderGroup"
      pattern: "moveFolder|restoreFolderGroup"
    - from: "web/src/components/LeftTree.tsx"
      to: "TreeContextMenu items-array"
      via: "menuItems(target) page/root branch"
      pattern: "menuItems"
---

<objective>
Pin the Clean-Rebuild Behavior Inventory (07-RESEARCH §) as a regression-test net FIRST, then cleanly rebuild `LeftTree`, `TreeContextMenu`, `RenameModal`, and `MoveDialog` from scratch (the deliberate CONTEXT override of the ROADMAP "do NOT re-do" note) — preserving EVERY currently-shipped behavior with no user-visible regression and adding NO new folder features yet. This produces a clean, maintainable base that Plan 04 layers folder DnD + optimistic updates + the folder context menu + DeleteFolderDialog + grouped TrashView onto. The regression net is the load-bearing safety mechanism: it must be green against the CURRENT components before a single rebuild edit, and green again after the rebuild.

Purpose: A clean tree base with the page-side context menu (TREE-01 page side) and zero regression — the precondition for safely adding folder operations.
Output: `web/src/components/__regression__/treeBehaviors.test.tsx` (pinned first); rebuilt LeftTree/TreeContextMenu/RenameModal/MoveDialog; LeftTree.test.tsx mock factory extended with the four new client functions so the rebuilt component imports cleanly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-obsidian-style-file-tree-folder-operations-tree-ux/07-CONTEXT.md
@.planning/phases/07-obsidian-style-file-tree-folder-operations-tree-ux/07-RESEARCH.md
@.planning/phases/07-obsidian-style-file-tree-folder-operations-tree-ux/07-PATTERNS.md
@.planning/phases/07-obsidian-style-file-tree-folder-operations-tree-ux/07-UI-SPEC.md
@.planning/phases/07-obsidian-style-file-tree-folder-operations-tree-ux/07-VALIDATION.md

# Client functions added in 07-01/07-02 — the mock factory must list them.
@.planning/phases/07-obsidian-style-file-tree-folder-operations-tree-ux/07-01-PLAN.md
@.planning/phases/07-obsidian-style-file-tree-folder-operations-tree-ux/07-02-PLAN.md

# Frontend analogs to mirror exactly (the rebuild reproduces these behaviors)
@web/src/components/LeftTree.tsx
@web/src/components/LeftTree.test.tsx
@web/src/components/TreeContextMenu.tsx
@web/src/components/TreeContextMenu.test.tsx
@web/src/components/RenameModal.tsx
@web/src/components/MoveDialog.tsx
@web/src/components/DeleteConfirmDialog.tsx
@web/src/components/Dialog.tsx
@web/src/components/LeftTree.css
@web/src/components/TreeContextMenu.css
@web/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the Clean-Rebuild Behavior Inventory regression net (BEFORE any rebuild)</name>
  <files>web/src/components/__regression__/treeBehaviors.test.tsx, web/src/components/LeftTree.test.tsx</files>
  <read_first>
    - 07-RESEARCH.md §Clean-Rebuild Behavior Inventory (the 14 LeftTree items + TreeContextMenu a11y + RenameModal/MoveDialog/DeleteConfirmDialog/TrashView/Dialog contracts)
    - web/src/components/LeftTree.test.tsx (existing render harness + vi.mock factory at lines 11-21)
    - web/src/components/TreeContextMenu.test.tsx (existing a11y assertions to mirror)
    - web/src/components/LeftTree.tsx (current behavior being pinned)
  </read_first>
  <action>
    Create web/src/components/__regression__/treeBehaviors.test.tsx pinning the shipped behaviors as black-box tests against the CURRENT components (so the net is green BEFORE the rebuild and stays the contract). Cover, at minimum: (1) null/undefined tree coalesces to [] (no white-screen); (2) folders expand/collapse with aria-expanded; (3) active page row gets navrow-active + aria-current="page"; (4) right-click a page (editor) shows Rename/Move/Version history/Delete; right-click a page (reader) shows ONLY Version history; (5) right-click a folder (editor) shows the create actions; right-click blank space shows root create; (6) page rows draggable (editor) set application/x-okf-page on dragStart; (7) drop a page onto a folder row moves it (movePage called); same-parent drop is a no-op; (8) loading role="status" / error role="alert" copy. Also extend the EXISTING LeftTree.test.tsx vi.mock("../api/client") factory (lines 11-21) to ALSO export renameFolder, moveFolder, deleteFolder, restoreFolderGroup as vi.fn() — the rebuilt component (Task 2+) will import these, and an incomplete factory throws on import (RESEARCH Runtime State). Run the net against the current components now — it MUST be green before Task 2 touches any source.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace/web && npx vitest run src/components/__regression__/treeBehaviors.test.tsx src/components/LeftTree.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>treeBehaviors.test.tsx exists and is GREEN against the current (un-rebuilt) components; LeftTree.test.tsx mock factory lists renameFolder/moveFolder/deleteFolder/restoreFolderGroup; the net encodes the shipped page menu, page DnD, folder-scoped create, loading/error, and a11y behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Clean rebuild — LeftTree + TreeContextMenu (preserve all shipped behavior, no new features)</name>
  <files>web/src/components/LeftTree.tsx, web/src/components/LeftTree.css, web/src/components/TreeContextMenu.tsx, web/src/components/TreeContextMenu.css</files>
  <read_first>
    - web/src/components/LeftTree.tsx (whole file — the 14-item behavior inventory to reproduce)
    - web/src/components/TreeContextMenu.tsx (whole file — role=menu/menuitem, focus trap, arrow/Home/End, outside-click/scroll/resize close, 4px clamp)
    - 07-UI-SPEC.md §Tree & Folder-Operations Interaction Contract (visual/interaction contract; tokens only, no new tokens)
    - web/src/components/LeftTree.css, TreeContextMenu.css (token-only CSS to preserve)
  </read_first>
  <action>
    Rewrite LeftTree.tsx and TreeContextMenu.tsx cleanly for maintainability while reproducing EVERY behavior pinned in Task 1 — and NO new folder feature (folder DnD, optimistic updates, folder context-menu mutate actions, and DeleteFolderDialog all land in Plan 04). Decompose for clarity: keep the forwardRef LeftTreeHandle (openCreatePage/openCreateFolder), the nodes = data ?? [] null-coalesce, parentOf same-parent no-op guard, canEdit RBAC, the menuItems(target) page/folder(create-only)/root branch, the TreeRow recursion with expand/collapse + active-row highlight, the existing application/x-okf-page dragStart + folder-row/root drop zones, and the .lefttree-droptarget highlight. The moveMut stays the SHIPPED onSuccess-invalidate form for now (the optimistic onMutate/onError/onSettled upgrade is Plan 04 — do NOT add it here). TreeContextMenu keeps its items-array {label,onSelect,danger?} contract and all a11y verbatim. CSS references tokens.css variables only (no hard-coded hex/px); introduce NO new tokens. The components must keep importing only the client functions they currently use plus tolerate the now-present folder functions (no call sites yet). Confirm the regression net + existing LeftTree/TreeContextMenu tests stay green.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace/web && npx tsc --noEmit 2>&1 | tail -3 && npx vitest run src/components/__regression__/treeBehaviors.test.tsx src/components/LeftTree.test.tsx src/components/TreeContextMenu.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>LeftTree.tsx + TreeContextMenu.tsx are rebuilt; the regression net + LeftTree.test.tsx + TreeContextMenu.test.tsx are all green; no new folder feature added; CSS is token-only with no new tokens; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Clean rebuild — RenameModal + MoveDialog parameterized by node kind (page path green)</name>
  <files>web/src/components/RenameModal.tsx, web/src/components/MoveDialog.tsx</files>
  <read_first>
    - web/src/components/RenameModal.tsx (Dialog shell, empty-title validation, onSuccess invalidate ["tree"]+["page"] + navigate, help copy)
    - web/src/components/MoveDialog.tsx (collectFolders flatten → select + "Top level", movePage mutation, help copy)
    - 07-UI-SPEC.md §Copywriting Contract (page strings unchanged; folder strings are the net-new copy 04 will use)
    - web/src/api/client.ts (renamePage/movePage and the 07-01 renameFolder/moveFolder signatures)
  </read_first>
  <action>
    Rebuild RenameModal.tsx and MoveDialog.tsx as the SHARED dialogs, parameterized by a node `kind: "page" | "folder"` prop (default/explicit "page" for now) so Plan 04 can pass "folder" with zero further refactor. For kind "page" the behavior is IDENTICAL to today: RenameModal calls renamePage with empty-title client validation ("Give your page a title."), onSuccess invalidates ["tree"]+["page"], closes, navigates to the new path, help "Links to this page will keep working.", confirm "Rename"; MoveDialog flattens the tree to a folder <select> with the "Top level" ("") option, calls movePage, onSuccess invalidate + navigate, help "Choose where this page should live." / "Links to this page will keep working.", confirm "Move page". Wire the folder branch to call renameFolder/moveFolder and surface a 409 (err.status===409) as a non-fatal role="alert" field error using the UI-SPEC collision copy "A folder with that name already exists there. Pick a different name or destination." so the dialog stays open — but leave the folder branch UNREACHED from LeftTree until Plan 04 opens these dialogs for folders. Reuse the existing Dialog shell, focus trap, Esc/backdrop cancel (never confirms), and .dialog-footer layout (the shipped dialog-footer fix). Keep the page-path tests green.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace/web && npx tsc --noEmit 2>&1 | tail -3 && npx vitest run src/components/RenameModal src/components/MoveDialog src/components/__regression__/treeBehaviors.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>RenameModal + MoveDialog are rebuilt and parameterized by node kind; the page path is byte-for-byte behavior-identical and green; the folder branch exists (calls renameFolder/moveFolder, 409→collision copy, dialog stays open) but is not yet opened for folders; tsc passes; regression net green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| folder/page title → rendered tree row, menu, dialog | Titles originate from page frontmatter / folder names (user content). |
| client component → API | Mutations carry path + destination; the server is authoritative (RBAC + path safety in 07-01/07-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering (stored XSS) | folder/page title surfaced in tree rows, context menu, dialogs | mitigate | Titles render as React text nodes only (no dangerouslySetInnerHTML); the rebuild preserves this — no raw HTML from names. Read mode sanitization (Phase 1) is unaffected. |
| T-07-10 | EoP (client-side RBAC illusion) | reader sees mutate menu items | mitigate | canEdit hides items client-side AND the server editor gate (07-01/07-02) is authoritative; the menu is convenience, not the control. |
| T-07-SC | Tampering | npm installs | accept | Zero packages installed; native HTML5 DnD + existing deps only (UI-SPEC forbids a DnD library). |
</threat_model>

<verification>
- Regression net `treeBehaviors.test.tsx` green BEFORE Task 2 edits and green after the full rebuild.
- `cd web && npm test` green (all component tests, including LeftTree/TreeContextMenu/RenameModal/MoveDialog/TrashView).
- `cd web && npx tsc --noEmit` passes.
- No new tokens added to tokens.css; CSS references variables only.
</verification>

<success_criteria>
- The Clean-Rebuild Behavior Inventory is pinned by a regression net and stays green across the rebuild (no user-visible regression).
- LeftTree/TreeContextMenu/RenameModal/MoveDialog are cleanly rebuilt; the page-side context menu (TREE-01 page side) is preserved.
- No new folder feature is introduced in this plan; the base is clean for Plan 04.
- The mock factory lists the 07-01/07-02 client functions so the rebuilt components import without throwing.
</success_criteria>

<output>
Create `.planning/phases/07-obsidian-style-file-tree-folder-operations-tree-ux/07-03-SUMMARY.md` when done.
</output>
