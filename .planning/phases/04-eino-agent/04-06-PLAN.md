---
phase: 04-eino-agent
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - web/src/api/client.ts
  - web/src/stores/agentPanel.ts
  - web/src/components/PromptBar.tsx
  - web/src/components/PromptBar.css
  - web/src/components/AgentPanel.tsx
  - web/src/components/AgentPanel.css
  - web/src/components/AgentAnswer.tsx
  - web/src/components/DiffReviewDialog.tsx
  - web/src/components/DiffReviewDialog.css
  - web/src/components/DiffReviewDialog.test.tsx
  - web/src/styles/tokens.css
  - web/src/routes/AppShell.tsx
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04, AGNT-05, AGNT-06, AGNT-07, AGNT-08, AGNT-09, AGNT-10]
must_haves:
  truths:
    - "A persistent bottom PromptBar lets a user pick a mode (Ask/Summarize/Rewrite/Draft/Propose) and submit; context auto-detects (page/selection/attachment) with a Whole-workspace toggle"
    - "Ask answers stream token-by-token into a right-side collapsible AgentPanel (SSE via a fetch-stream reader)"
    - "A DiffReviewDialog renders a REAL diff (never prose) with Approve & save / Reject; Approve is NOT auto-focused; the stale state blocks approve"
    - "Approving a patch POSTs /apply-patch; a 409 surfaces the 'page changed, re-run' stale state; reject discards"
    - "When the agent is off or the provider is unreachable, the PromptBar renders disabled with an inline explanation (never a silent hang)"
  artifacts:
    - path: "web/src/components/DiffReviewDialog.tsx"
      provides: "real-diff approval modal on the Dialog shell (reused in Phase 5); props {title,oldText,newText,summary?,onApprove,onReject,stale?,busy?}"
      contains: "react-diff-viewer-continued"
    - path: "web/src/components/DiffReviewDialog.test.tsx"
      provides: "renders a real diff (never prose); Approve not auto-focused; stale blocks approve"
      contains: "Approve"
    - path: "web/src/api/client.ts"
      provides: "agent fetch-stream SSE consumer + proposePatch/applyPatch mutations (CSRF + 409 surfacing)"
      contains: "getReader"
    - path: "web/src/stores/agentPanel.ts"
      provides: "zustand+persist panel open/collapse store (mirrors editorMode.ts)"
      contains: "persist"
    - path: "web/src/styles/tokens.css"
      provides: "additive --agentpanel-width: 360px (only new token)"
      contains: "--agentpanel-width"
  key_links:
    - from: "web/src/components/AgentPanel.tsx"
      to: "web/src/api/client.ts"
      via: "subscribes the agent fetch-stream and appends tokens into AgentAnswer"
      pattern: "agent"
    - from: "web/src/components/DiffReviewDialog.tsx"
      to: "web/src/components/Dialog.tsx"
      via: "built ON Dialog (focus trap, backdrop-never-confirms); initial focus on Reject not Approve"
      pattern: "Dialog"
    - from: "web/src/api/client.ts"
      to: "internal/server/handlers_agent.go"
      via: "POST /agent/apply-patch with CSRF; 409 → stale-revision UI"
      pattern: "apply-patch"
  prohibitions:
    - "DiffReviewDialog NEVER shows prose-only — the diff component is always rendered (no-op shows 'No changes were proposed.' + disabled Approve)"
    - "Approve is primary/accent but MUST NOT be auto-focused — initial focus lands on Reject / the diff scroll region (trust contract)"
    - "NO Git vocabulary in any agent UI ('commit'/'repo'/'push'/'SHA' forbidden) — use 'Approve & save' / 'Change saved'"
    - "Streamed answers render through the existing sanitized MarkdownProse surface (rehype-raw OFF) — no raw HTML / no stored-XSS regression"
    - "Only ONE new token (--agentpanel-width); no new color/type/spacing tokens; token-only styling (no hard-coded hex/px where a token exists)"
    - "The propose/apply (Approve) affordances render only for editor/admin role; readers may Ask/Summarize but never reach Approve"
---

<objective>
Slice 6 of 6 (frontend, after its endpoints): wire `@tanstack/react-query` mutations + an agent fetch-stream SSE consumer, and build the three agent surfaces per the UI-SPEC — the persistent bottom PromptBar (mode + auto-context + workspace toggle + submit + disabled states), the right-side collapsible AgentPanel + AgentAnswer (streamed, sanitized render), and the load-bearing DiffReviewDialog (real diff, Approve-not-auto-focused, stale-blocks-approve) reused in Phase 5.

Purpose: the user-facing half of AGNT-01..AGNT-10 — Ask/Summarize/Rewrite/Draft/Propose entry, streamed answers, and the trust gate. After this slice the agent is usable end-to-end and the DiffReviewDialog trust contract is enforced + tested.
Output: client.ts agent calls, agentPanel store, PromptBar/AgentPanel/AgentAnswer/DiffReviewDialog (+ CSS + the dialog test), the one additive token, and AppShell wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-eino-agent/04-UI-SPEC.md
@.planning/phases/04-eino-agent/04-PATTERNS.md
@.planning/phases/04-eino-agent/04-05-SUMMARY.md

# Existing seams (read cited ranges only)
@web/src/api/client.ts
@web/src/components/Dialog.tsx
@web/src/components/MarkdownProse.tsx
@web/src/stores/editorMode.ts
@web/src/routes/AppShell.tsx
@web/src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent client calls (fetch-stream SSE + propose/apply mutations) + panel store + token</name>
  <files>web/src/api/client.ts, web/src/stores/agentPanel.ts, web/src/styles/tokens.css</files>
  <read_first>
    PATTERNS.md §"web/src/api/client.ts" — reuse `mutate<T>()` (lines 50-85: credentials same-origin + ensureCSRF + X-CSRF-Token + throws Error&{status?} so a 409 surfaces as err.status===409, exactly like savePage at 248-253) for proposePatch/applyPatch; the agent chat stream is an authenticated POST-with-body token stream → `subscribeExtractionStatus` (EventSource, lines 459-479) is the lifecycle/cleanup MODEL ONLY — the agent needs a NEW fetch-stream reader (`fetch(..., {body}).then(r => r.body.getReader())`) per RESEARCH §Item 6 (EventSource can't send a body or CSRF). §"web/src/stores/agentPanel.ts" — copy editorMode.ts (lines 22-31) create+persist shape, key "okf.agent.panelOpen". UI-SPEC §"Net-New Token Proposal" — add `--agentpanel-width: 360px` in the tokens.css "Layout fixed dimensions" block alongside --navrail-width (the ONLY new token). Note: QueryClientProvider is ALREADY wired in web/src/main.tsx — use react-query directly, no provider scaffolding needed.
  </read_first>
  <action>
    In `web/src/api/client.ts` add: a fetch-stream agent chat consumer (POST the prompt+scope, read `res.body.getReader()`, decode SSE `data:` frames, invoke an onToken callback per delta, return an abort/unsubscribe that cancels the fetch — mirror subscribeExtractionStatus's cleanup shape) and `proposePatch`/`applyPatch` mutations via the existing `mutate()` helper (applyPatch surfaces a 409 as err.status===409 for the stale UI). Create `web/src/stores/agentPanel.ts` as a zustand+persist store (open/collapse boolean, key "okf.agent.panelOpen") copying the editorMode.ts pattern. Add the single additive token `--agentpanel-width: 360px` to the Layout fixed dimensions block in `web/src/styles/tokens.css` — no other token changes.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace/web && npx tsc --noEmit && grep -q 'getReader' src/api/client.ts && grep -q -- '--agentpanel-width' src/styles/tokens.css && grep -q 'okf.agent.panelOpen' src/stores/agentPanel.ts && echo OK</automated>
  </verify>
  <done>client.ts has a fetch-stream agent consumer (body + CSRF, not EventSource) + proposePatch/applyPatch with 409 surfacing; agentPanel store persists open/collapse; the one additive --agentpanel-width token exists; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: DiffReviewDialog (real diff, trust contract) + its test</name>
  <files>web/src/components/DiffReviewDialog.tsx, web/src/components/DiffReviewDialog.css, web/src/components/DiffReviewDialog.test.tsx</files>
  <read_first>
    UI-SPEC §"DiffReviewDialog — Trust Contract" (MANDATORY: diff component always rendered; no-op → "No changes were proposed." + disabled Approve; Approve is `.btn-primary` accent labeled "Approve & save" but NOT auto-focused — initial focus on Reject/diff scroll region; Reject = secondary/destructive-text "Reject"; backdrop/Esc = cancel = reject; busy → "Saving…"; stale state replaces Approve with the warning banner + Re-run/Close) and §"Copywriting Contract" (exact strings; NO Git vocabulary). PATTERNS.md §"web/src/components/DiffReviewDialog.tsx" — built ON Dialog.tsx (focus trap, backdrop-never-confirms lines 9-10/103-105); the DEVIATION: do NOT auto-focus Approve (Dialog focuses first focusable at lines 58-61 → put Reject first / focus the scroll region). Diff body = react-diff-viewer-continued (themed via styles prop to tokens; added from --color-success, removed from --color-destructive, low saturation; mono). VALIDATION.md "Wave 0" + the dialog test requirements.
  </read_first>
  <action>
    Create `web/src/components/DiffReviewDialog.tsx` (+ co-located `.css`) on the existing Dialog shell with props {title, oldText, newText, summary?, onApprove, onReject, stale?, busy?}. Always render react-diff-viewer-continued (split view default + inline toggle, mono, line numbers, added/removed tints derived from --color-success/--color-destructive at low saturation — diff semantics, not the 10% accent). If oldText===newText, show "No changes were proposed." and disable Approve. Footer: Reject (secondary/destructive-text, "Reject") left, Approve ("Approve & save", .btn-primary accent) right — Approve is NOT auto-focused (land initial focus on Reject or the diff scroll region; add a code comment so a future refactor doesn't "fix" it). Busy → "Saving…". Stale (stale===true) → replace the Approve path with the AlertTriangle warning banner ("This page changed since the assistant read it." + body) and Re-run/Close, never accent. Token-only styling; NO Git vocabulary. Create `web/src/components/DiffReviewDialog.test.tsx` (Vitest + testing-library) asserting: the diff renders for differing old/new (never prose-only); Approve is not the initially-focused element; the stale state blocks/removes Approve; the no-op shows "No changes were proposed." with Approve disabled.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace/web && npx tsc --noEmit && npx vitest run src/components/DiffReviewDialog && echo OK</automated>
  </verify>
  <done>DiffReviewDialog renders a real diff (never prose-only), Approve is accent-primary but not auto-focused, the stale state blocks approve, no-op disables Approve; copy carries no Git vocabulary; the Vitest test asserts all four trust behaviors and passes.</done>
</task>

<task type="auto">
  <name>Task 3: PromptBar + AgentPanel + AgentAnswer + AppShell wiring</name>
  <files>web/src/components/PromptBar.tsx, web/src/components/PromptBar.css, web/src/components/AgentPanel.tsx, web/src/components/AgentPanel.css, web/src/components/AgentAnswer.tsx, web/src/routes/AppShell.tsx</files>
  <read_first>
    UI-SPEC §"Surface Layout & Placement" (PromptBar = a third bottom row of the appshell flex column, full-width, --color-surface + top border; AgentPanel = a third right column flex:0 0 var(--agentpanel-width), left border mirroring .navrail, collapsible via the zustand store + a topbar toggle, auto-open on first submit; responsive overlay <1280px / collapsed <900px), §"PromptBar States" + §"Context Auto-Detection" (mode select, read-only context chip page/selection/attachment, Whole-workspace toggle; disabled agent-off/provider-unreachable states; Enter submits / Shift+Enter newline / Esc cancels), §"AgentPanel States" (empty/loading/streaming/done/error; citation line on workspace; "Propose this as a patch" footer for editor+page; aria-live coalesced), §"Copywriting Contract" + §"Accessibility". PATTERNS.md §"PromptBar/AgentPanel/AgentAnswer" — analogs AppShell.tsx chrome, controls.css (.btn/.select/.input), MarkdownProse.tsx (sanitized read-only render for streamed answers, rehype-raw OFF), AutosaveStatus (.spinner + aria-live). Editor-gating: propose/Approve render only when meData.role is editor/admin (the existing gate).
  </read_first>
  <action>
    Create `web/src/components/PromptBar.tsx` (+ .css): single-row mode `<select>` (Ask/Summarize/Rewrite/Draft/Propose a patch) + read-only AgentContextChip (auto page/selection/attachment, overridden by the Whole-workspace `aria-pressed` toggle) + Submit (.btn-primary, SendHorizontal); mode-aware placeholder + submit label; Enter submits / Shift+Enter newline / Esc cancels in-flight; disabled+explanation when agent.enabled===false or provider unreachable (fail-closed, never hang); in-flight spinner ("Thinking…"→"Streaming…") via the AutosaveStatus pattern; Rewrite needs an active selection, Propose needs page scope + editor role (unavailable modes are disabled options). Create `web/src/components/AgentPanel.tsx` (+ .css) as the right-side collapsible column (store-driven open/collapse, topbar toggle, auto-open on first submit, left border mirroring navrail, responsive overlay/collapse per UI-SPEC) with header ("Assistant" + mode·scope meta + collapse), the AgentAnswer region, and the editor+page "Propose this as a patch" footer (opens DiffReviewDialog). Create `web/src/components/AgentAnswer.tsx` rendering streamed Markdown through the SAME sanitized MarkdownProse surface (rehype-raw OFF), aria-live="polite" coalesced to settle boundaries, with the workspace "Reasoned over:" citation links and the error state that keeps the user's prompt. Wire all three into `web/src/routes/AppShell.tsx` (PromptBar bottom row, AgentPanel right column, the topbar panel toggle). Token-only styling; no Git vocabulary; only editor/admin sees propose/Approve.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace/web && npx tsc --noEmit && npx vitest run src/components/AgentPanel src/components/PromptBar src/components/AgentAnswer && echo OK</automated>
  </verify>
  <done>The PromptBar (modes + auto-context + workspace toggle + disabled agent-off state), AgentPanel (collapsible, streamed, citations, editor-gated propose footer), and AgentAnswer (sanitized aria-live render) are wired into AppShell; the frontend type-checks and the agent-component Vitest run (AgentPanel/PromptBar/AgentAnswer) passes (the full `npm test` suite stays the phase-level gate in VALIDATION.md, not duplicated here — it includes deferred Phase-6/7 browser tests that can spuriously fail); agent-off fails closed and readers never see Approve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| streamed model output → DOM | answer Markdown rendered in the browser; must go through the sanitized surface (no raw HTML). |
| Approve click → /apply-patch | the consequential write; CSRF + editor-gated + the real-diff gate; 409 surfaces stale. |
| agent-off / unreachable → UI | must render a disabled explanation, never a silent hang. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Tampering (stored XSS) | AgentAnswer render | mitigate | streamed answers render through the existing sanitized MarkdownProse surface (rehype-raw OFF) — no raw HTML reaches the DOM. CLAUDE.md stored-XSS guard; AI-SPEC V5. |
| T-04-22 | Tampering / EoP | Approve trust gate | mitigate | DiffReviewDialog always renders a REAL diff (never prose); Approve not auto-focused; backdrop/Esc = reject; stale blocks approve — the human gate against indirect injection. AGNT-10; AI-SPEC §1 #1/#3; tested in Task 2. |
| T-04-23 | EoP | reader reaching Approve | mitigate | propose/Approve affordances render only for editor/admin (the existing meData.role gate); readers Ask/Summarize only. AGNT-10; V4. |
| T-04-24 | Spoofing / CSRF | /apply-patch from the browser | mitigate | applyPatch uses the existing mutate() CSRF token + same-origin credentials; the backend endpoint carries nosurf. AI-SPEC V CSRF. |
| T-04-25 | Information Disclosure | "page changed" 409 handling | mitigate | a 409 surfaces the stale-revision UI ("re-run"), never silently retries the stale apply. AGNT-10; AI-SPEC §6. |
</threat_model>

<verification>
- `npx tsc --noEmit` green; the scoped agent-component Vitest run green (`npx vitest run src/components/AgentPanel src/components/PromptBar src/components/AgentAnswer`) plus the DiffReviewDialog test (Task 2). The full `npm test -- --run` is the phase-level gate in VALIDATION.md (it carries deferred Phase-6/7 browser tests that can spuriously fail), not a per-task verify here.
- grep confirms getReader (fetch-stream), --agentpanel-width, react-diff-viewer-continued, the panel store key.
- Manual (VALIDATION.md): Ask streams into the panel; propose → real diff → Approve saves (Change saved) / 409 shows the re-run stale state / Reject discards; agent-off disables the PromptBar with copy; no Git vocabulary anywhere.
</verification>

<success_criteria>
The PromptBar, AgentPanel/AgentAnswer, and DiffReviewDialog deliver the user-facing agent per the UI-SPEC: streamed Ask, sanitized answers, a real-diff trust gate (Approve not auto-focused, stale-blocks-approve, no Git vocabulary), CSRF-protected apply with 409 stale handling, editor-gated propose/Approve, and a fail-closed agent-off state — with exactly one additive token and the dialog trust contract under test.
</success_criteria>

<output>
Create `.planning/phases/04-eino-agent/04-06-SUMMARY.md` when done.
</output>
