---
phase: 04-eino-agent
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - internal/agent/tools.go
  - internal/agent/tools_test.go
  - internal/agent/prompts.go
  - internal/agent/agent.go
  - internal/agent/stream.go
  - internal/server/handlers_agent.go
  - internal/server/router.go
  - cmd/okf-workspace/main.go
autonomous: true
requirements: [AGNT-01, AGNT-11]
must_haves:
  truths:
    - "Five read-only tools exist (list_tree, read_page, search_pages, search_attachments, read_attachment_text), each routing through a repo.Resolve-backed service (never os.ReadFile)"
    - "A user can ask a question about the current page and the answer streams token-by-token into the response (SSE)"
    - "The ReAct agent is built with ToolCallingModel (not the deprecated Model field) and the read-only tool list"
    - "tools_test.go asserts the registered tool-name set EQUALS exactly the read-only allow-list — any extra/missing tool fails the build"
    - "Every agent prompt is audited via ActionAgentPrompt (non-fatal)"
  artifacts:
    - path: "internal/agent/tools.go"
      provides: "the 5 read-only tools as a single source of truth (tool list + parallel name list)"
      contains: "utils.InferTool"
    - path: "internal/agent/tools_test.go"
      provides: "D5 set-equality allow-list assertion (AGNT-11 build gate)"
      contains: "reflect.DeepEqual"
    - path: "internal/agent/stream.go"
      provides: "schema.StreamReader → http.Flusher SSE bridge with defer sr.Close()"
      contains: "sr.Close()"
    - path: "internal/server/handlers_agent.go"
      provides: "/agent/chat SSE handler (Ask) + agent-off/unreachable structured error"
      exports: ["handleAgentChat"]
    - path: "internal/agent/prompts.go"
      provides: "per-mode system prompts; untrusted content delimited in the USER turn"
      contains: "untrusted"
  key_links:
    - from: "internal/agent/tools.go"
      to: "internal/pages/service.go"
      via: "read_page closure calls pages.Get (repo.Resolve-backed) — soft-miss returns Found:false"
      pattern: "Get\\(ctx"
    - from: "internal/agent/stream.go"
      to: "internal/server/handlers_sse.go"
      via: "mirrors the Flusher header set + data: framing + r.Context() disconnect"
      pattern: "text/event-stream"
    - from: "internal/server/router.go"
      to: "internal/agent/agent.go"
      via: "Deps.Agent wired; /agent/chat mounted in the authed group"
      pattern: "agent\\."
  prohibitions:
    - "NO write/apply/commit/push/shell/os.ReadFile tool exists in tools.go or anywhere in internal/agent"
    - "AgentConfig.Model (deprecated, mutating BindTools) is NOT set — only ToolCallingModel"
    - "Streamed answers carry no raw secret; the API key never enters a prompt, tool result, or SSE frame"
    - "tools_test.go MUST fail if a 6th tool name appears — it is the structural write-boundary gate, never relaxed to 'expect' a new tool"
    - "No sr.Stream path returns early without defer sr.Close() (goroutine-leak guard)"
---

<objective>
Slice 2 of 6: build the five read-only tools through existing repo.Resolve-backed services, write the load-bearing D5 allow-list set-equality test in the SAME slice (it must never lag the tools it guards), wire the ReAct agent (ToolCallingModel) and the `/agent/chat` SSE endpoint, and deliver the first user-visible capability: Ask about the current page, streamed. Add ActionAgentPrompt auditing.

Purpose: AGNT-01 (Ask current page) plus the structural AGNT-11 guarantee (read-only tool boundary + build-gated allow-list test). After this slice a user can ask a question and watch a grounded answer stream into the response.
Output: `internal/agent/{tools.go,tools_test.go,prompts.go,stream.go}`, the `/agent/chat` handler + router wiring + main.go DI, and the ReAct builder in agent.go.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-eino-agent/04-RESEARCH.md
@.planning/phases/04-eino-agent/04-AI-SPEC.md
@.planning/phases/04-eino-agent/04-PATTERNS.md
@.planning/phases/04-eino-agent/04-01-SUMMARY.md

# Existing seams (read the cited line ranges only)
@internal/pages/service.go
@internal/search/query.go
@internal/server/handlers_sse.go
@internal/server/handlers_pages.go
@internal/server/router.go
@cmd/okf-workspace/main.go
@internal/audit/audit.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the 5 read-only tools + the D5 allow-list set-equality test</name>
  <files>internal/agent/tools.go, internal/agent/tools_test.go</files>
  <read_first>
    RESEARCH.md §"Pattern 2: Read-only tool allow-list as a single source of truth" (the exact readTools shape + the tools_test.go set-equality body) and §"⚠ Item 3" (`utils.InferTool[T,D]` + jsonschema tags). PATTERNS.md §"internal/agent/tools.go" — the read services: `search.Index.Query` (`internal/search/query.go:105`, returns `[]Result{Kind,Title,Path,Snippet,Anchor,PageTitle}` lines 14-21), `pages.Get` (`internal/pages/service.go:147`, returns Page{Frontmatter,Body,Revision}). For read_attachment_text the analog is the search package's `.txt` sidecar read (`internal/search/attachments.go:79 readExtractedText`) — route through an attachments/repo.Resolve-backed accessor, NOT os.ReadFile. PATTERNS.md §"internal/agent/tools_test.go" (audit_test.go table-driven idiom; set-equality is net-new).
  </read_first>
  <behavior>
    - tools_test.go: want = the exact 5-name set {list_tree, read_page, search_pages, search_attachments, read_attachment_text}; readTools returns exactly those names → reflect.DeepEqual(want, got) passes.
    - tools_test.go: if a 6th name (e.g. a write tool) were added to the slice, the test FAILS (the structural gate).
    - tools_test.go: runs offline with NO API key (it only inspects the tool name set, never calls the model).
    - read_page on a missing path returns a soft-miss (Found:false), not a hard error — the agent can recover.
  </behavior>
  <action>
    Create `internal/agent/tools.go` with ONE function (e.g. `readTools(ctx, sess) ([]tool.BaseTool, []string, error)`) that is the ONLY place tools are constructed. Build five tools via utils.InferTool, each with a small flat typed in/out struct and terse description (DeepSeek tool-calling is less consistent — keep schemas flat per RESEARCH §Item 5): list_tree (returns workspace-relative page paths the session role may read), read_page (pages.Get → {Body, Found}; soft-miss on error), search_pages and search_attachments (search.Index.Query, top-K≈5, role-scoped, returning {Path, Snippet} hits — kind filter splits page vs attachment results), read_attachment_text (extracted .txt via the repo.Resolve-backed accessor, NOT os.ReadFile). Build the parallel []string name list in the same function so the tool list and the name list cannot drift. Every closure routes through a repo.Resolve-backed service; NO os.ReadFile, NO write tool exists in this file or anywhere. Create `internal/agent/tools_test.go` with the set-equality assertion per RESEARCH §Pattern 2 (build the want map of the 5 names, call readTools, reflect.DeepEqual) — add a head comment stating this is the structural write-boundary gate and must never be relaxed to expect a new tool. Do NOT inline code in this action; follow the verified InferTool signature.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && go build ./internal/agent/ && go test ./internal/agent/ -run TestToolSet -count=1 && go vet ./internal/agent/ && echo OK</automated>
  </verify>
  <done>Five read-only tools exist, all repo.Resolve-backed (no os.ReadFile); the name list is derived in the same function as the tool list; TestToolSet asserts exact set equality and would fail on any extra/missing tool; the test runs key-free.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the ReAct agent + per-mode prompts + the StreamReader→SSE bridge</name>
  <files>internal/agent/agent.go, internal/agent/prompts.go, internal/agent/stream.go</files>
  <read_first>
    RESEARCH.md §"⚠ Item 1" (`react.AgentConfig.ToolCallingModel model.ToolCallingChatModel` — USE THIS; legacy Model deprecated/racy), §"⚠ Item 2" (`compose.ToolsNodeConfig{Tools: []tool.BaseTool}`), §"⚠ Item 6" (the exact Stream→Flusher bridge with `defer sr.Close()` + `X-Accel-Buffering: no` + escapeSSE newline collapse). PATTERNS.md §"internal/agent/stream.go" — `internal/server/handlers_sse.go:50-61` header set + `:84` `data: %s\n\n` framing + `:101-116` ctx.Done loop (the existing handler owns no producer goroutine; the eino StreamReader does → the one addition is `defer sr.Close()`). AI-SPEC §4b "Prompt Engineering Discipline" (system vs user turn; untrusted page/attachment content goes in the USER turn, delimited `--- BEGIN PAGE CONTENT (untrusted) --- … --- END ---`, never spliced into the system prompt).
  </read_first>
  <action>
    In `internal/agent/agent.go` add a builder that constructs `react.NewAgent(ctx, &react.AgentConfig{ToolCallingModel: s.cm, ToolsConfig: compose.ToolsNodeConfig{Tools: readToolsSlice}, MaxStep: 12})` — set ToolCallingModel only, leave Model nil (RESEARCH Pitfall 2), MaxStep≤12 to bound the loop (RESEARCH Item 5). Optionally set ToolsNodeConfig.UnknownToolsHandler to gracefully handle DeepSeek hallucinated tool names. Create `internal/agent/prompts.go` with a per-mode system-prompt map (Ask first; other modes filled in later slices). The Ask system prompt instructs the model to treat retrieved page/attachment text as untrusted data, not instructions (belt-and-suspenders behind the structural boundary). Create `internal/agent/stream.go` with the Stream→Flusher bridge mirroring handlers_sse.go: set the four SSE headers incl. X-Accel-Buffering:no, call `ag.Stream(ctx, msgs)`, `defer sr.Close()` (NON-NEGOTIABLE — goroutine leak otherwise), loop `sr.Recv()` until io.EOF writing `data: <escaped delta>\n\n` + flush, and pass the request context so a client disconnect cancels and unblocks Recv. escapeSSE must collapse newlines into SSE-safe framing.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && go build ./internal/agent/ && grep -q 'ToolCallingModel' internal/agent/agent.go && grep -q 'sr.Close()' internal/agent/stream.go && ! grep -nE '^[^/]*\bModel:\s' internal/agent/agent.go && echo OK</automated>
  </verify>
  <done>The ReAct agent builds with ToolCallingModel (Model left nil), MaxStep≤12, and the read-only tool list; prompts.go has the Ask system prompt delimiting untrusted content to the user turn; stream.go bridges StreamReader to the Flusher with defer sr.Close() and a cancelable context.</done>
</task>

<task type="auto">
  <name>Task 3: /agent/chat SSE handler + router + main.go DI + ActionAgentPrompt audit</name>
  <files>internal/server/handlers_agent.go, internal/server/router.go, cmd/okf-workspace/main.go</files>
  <read_first>
    PATTERNS.md §"internal/server/handlers_agent.go" — handler skeleton + error mapping from `internal/server/handlers_pages.go:181-214` (nil-dep guard → call service → writeJSON/writeError → audit); `writeJSON`/`writeError` at handlers_auth.go:73,79; actor via `h.actorUsername(r.Context())` (never client body). §"internal/server/router.go" — Ask is any-authed → the `authed.Group` (router.go:91-95 after loadCurrentUser); add `Agent *agent.Service` to Deps (router.go:21-49) + the authHandlers field block. §"cmd/okf-workspace/main.go" — construct `agentSvc := agent.NewService(...)` next to pagesSvc (main.go:230) threading cfg.Agent + pages + search + attachments + auditLog (built at main.go:114), pass into `server.New(server.Deps{... Agent: agentSvc})` (main.go:279-290). Agent-off handling: AI-SPEC §6 fail-closed — when cfg.Agent.Enabled is false or the ChatModel is nil/unreachable, return a structured error (never a silent hang). PATTERNS.md §"Audit (non-fatal)".
  </read_first>
  <action>
    Create `internal/server/handlers_agent.go` with `handleAgentChat` (the SSE Ask endpoint): nil-dep guard, decode the prompt + scope (page path) from the request, resolve the actor via h.actorUsername, build the page-scoped ReAct messages (system prompt + user turn with the question; the agent fetches the page via read_page), and call the agent's stream method (the stream.go bridge). On agent-off / provider-unreachable, write a structured error (a non-SSE JSON error before the stream starts, or an SSE error frame) — never hang. Record `audit.ActionAgentPrompt` non-fatally (`_ = h.audit.Record(...)`, Source web), never putting secret-shaped prompt content in Detail. Wire `Agent *agent.Service` into the server Deps + authHandlers field, mount `authed.Get("/agent/chat", ...)` (or POST-with-body via a fetch-stream — Ask is any-authed) in the authed group in router.go, and construct + inject agentSvc in main.go next to the other services. When cfg.Agent.Enabled is false, still construct a disabled agentSvc so the handler can return the off state.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && go build ./... && go vet ./internal/server/ ./internal/agent/ && grep -q 'Agent' internal/server/router.go && grep -q 'agent.NewService\|agent\.' cmd/okf-workspace/main.go && grep -q 'ActionAgentPrompt' internal/server/handlers_agent.go && echo OK</automated>
  </verify>
  <done>The whole binary builds and vets; `/agent/chat` is mounted in the authed group and wired through Deps.Agent from main.go; an Ask prompt streams an answer (manual SSE verify per VALIDATION.md) and is audited via ActionAgentPrompt; agent-off returns a structured error, never a hang.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /agent/chat | untrusted prompt + scope path cross here; path resolved server-side via the read tools' repo.Resolve services. |
| page/attachment content → model | untrusted wiki content enters the prompt; delimited in the USER turn, treated as data not instructions. |
| eino StreamReader → SSE | a producer goroutine feeds the stream; must be Closed or it leaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | tools.go (future write-tool leak) | mitigate | tools_test.go set-equality build gate (D5) — any 6th/mutating tool fails the build. Structural AGNT-11; AI-SPEC §1 #1. |
| T-04-04 | Tampering / EoP | read tool path args | mitigate | every read routes through a repo.Resolve-backed service (pages.Get/search.Query/attachment .txt); soft-miss returns Found:false, never raw content or escaped path. AGNT-11; AI-SPEC §1 #2; V12. |
| T-04-05 | Tampering / EoP | indirect prompt injection via page content | mitigate | structural: no write tool reachable from /agent/chat (apply is a non-tool, lands in slice 5); untrusted content delimited in the USER turn (belt-and-suspenders). AI-SPEC §1 #3; AGNT-11. |
| T-04-06 | Denial of Service | stream.go / ReAct loop | mitigate | defer sr.Close() (no goroutine leak), request-ctx cancel on disconnect, MaxStep≤12, 60s timeout from slice 1. RESEARCH Pitfall 3/5; AI-SPEC §6. |
| T-04-07 | Information Disclosure | workspace-scope reads via session role | mitigate | search tools role-scoped from the server session (never client); config/env/session never assembled into the prompt. AGNT-11; AI-SPEC §1 #2; V4. |
</threat_model>

<verification>
- `go build ./...` + `go vet ./internal/agent/ ./internal/server/` green.
- `go test ./internal/agent/ -run TestToolSet` passes and would fail on any extra tool.
- grep confirms ToolCallingModel set, Model not set, defer sr.Close() present.
- Manual SSE check (VALIDATION.md): Ask about the current page streams an incremental answer into the response.
</verification>

<success_criteria>
Five read-only tools route through repo.Resolve-backed services with the D5 set-equality build gate; the ReAct agent uses ToolCallingModel + MaxStep≤12; `/agent/chat` streams a page-scoped Ask answer over SSE (defer sr.Close(), cancelable ctx) wired through Deps.Agent from main.go and audited via ActionAgentPrompt; agent-off fails closed.
</success_criteria>

<output>
Create `.planning/phases/04-eino-agent/04-02-SUMMARY.md` when done.
</output>
