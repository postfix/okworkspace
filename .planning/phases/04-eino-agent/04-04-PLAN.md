---
phase: 04-eino-agent
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - internal/agent/agent.go
  - internal/agent/prompts.go
  - internal/agent/propose.go
  - internal/agent/propose_validate_test.go
  - internal/agent/dispatch_test.go
  - internal/server/handlers_agent.go
  - internal/server/router.go
autonomous: true
requirements: [AGNT-05, AGNT-06, AGNT-07, AGNT-08]
must_haves:
  truths:
    - "A user can ask the agent to summarize a page (single-shot, no tool round-trips)"
    - "A user can ask the agent to summarize an attachment (single-shot over extracted text)"
    - "A user can ask the agent to rewrite selected text and receive a rewritten span the server can diff against the original selection"
    - "A user can ask the agent to draft a new page and receive a full Markdown body that opens in the editor pending an explicit save"
    - "Free-form mode outputs are validated (validateProposedBody) and retried up to 2x; an invalid output never reaches the user"
    - "The single-shot dispatch picks the correct path per mode (Summarize vs Rewrite vs Draft), runs Rewrite/Draft through validateProposedBody, and sets the ~60s timeout — proven by a key-free test with a fake model (no network)"
  artifacts:
    - path: "internal/agent/agent.go"
      provides: "single-shot Summarize/Rewrite/Draft via ChatModel.Generate (no ReAct loop)"
      contains: "Generate"
    - path: "internal/agent/propose.go"
      provides: "validateProposedBody (rejects empty / ```-fenced / frontmatter-mangled bodies) + retry wrapper (≤2)"
      contains: "validateProposedBody"
    - path: "internal/server/handlers_agent.go"
      provides: "/summarize-page, /summarize-attachment endpoints (+ rewrite/draft dispatch)"
      contains: "summarize"
    - path: "internal/agent/dispatch_test.go"
      provides: "key-free dispatch test (fake model.ToolCallingChatModel stub) asserting per-mode single-shot path, Rewrite/Draft → validateProposedBody, and the ~60s timeout context — runs without DEEPSEEK_API_KEY"
      contains: "TestDispatch"
  key_links:
    - from: "internal/agent/agent.go"
      to: "internal/agent/propose.go"
      via: "Rewrite/Draft results pass through validateProposedBody + retry before returning"
      pattern: "validateProposedBody"
    - from: "internal/server/router.go"
      to: "internal/server/handlers_agent.go"
      via: "summarize-page/attachment are any-authed reads; rewrite/draft dispatch wired"
      pattern: "summarize"
    - from: "internal/agent/dispatch_test.go"
      to: "internal/agent/agent.go"
      via: "fake model.ToolCallingChatModel stub drives the dispatch and asserts per-mode path + validateProposedBody + 60s timeout, key-free"
      pattern: "TestDispatch"
  prohibitions:
    - "Summarize/Rewrite/Draft use single-shot ChatModel.Generate — NOT the ReAct loop (fewer round-trips, fewer failure modes per CONTEXT decision)"
    - "Draft and Rewrite NEVER auto-write — Draft opens in the editor pending explicit save; Rewrite returns a proposal for the diff dialog (slice 5/6)"
    - "A body wrapped in ``` fences, empty, or with mangled/reordered frontmatter is rejected by validateProposedBody, not surfaced"
    - "Every Generate call is wrapped in context.WithTimeout(60s) with MaxTokens set (never unbounded)"
---

<objective>
Slice 4 of 6: deliver the four single-shot modes — Summarize page, Summarize attachment, Rewrite selection, Draft new page — via direct `ChatModel.Generate` (no ReAct loop, per the locked hybrid decision), and build the `validateProposedBody` + retry harness that every body-producing mode (Rewrite/Draft here, Propose-patch in slice 5) depends on.

Purpose: AGNT-05/06 (summarize page/attachment), AGNT-07 (rewrite selection → proposal), AGNT-08 (draft new page). After this slice a user can summarize, rewrite a selection, and draft a page; rewrite returns a clean proposal the slice-5/6 diff dialog can render.
Output: single-shot dispatch in agent.go, the mode system prompts in prompts.go, `internal/agent/propose.go` (validate + retry), the summarize endpoints + rewrite/draft dispatch in handlers_agent.go, and a key-free `internal/agent/dispatch_test.go` (`TestDispatch`) proving the per-mode dispatch, the validateProposedBody pass-through, and the 60s timeout with a fake model (no network).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-eino-agent/04-RESEARCH.md
@.planning/phases/04-eino-agent/04-AI-SPEC.md
@.planning/phases/04-eino-agent/04-PATTERNS.md
@.planning/phases/04-eino-agent/04-03-SUMMARY.md

# Existing seams (read cited ranges only)
@internal/pages/service.go
@internal/okf/okf.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validateProposedBody + retry harness (the body-output contract)</name>
  <files>internal/agent/propose.go, internal/agent/propose_validate_test.go</files>
  <read_first>
    AI-SPEC §4b "Structured Outputs" — the `proposeWithRetry` shape (≤2 retries = 3 attempts; log attempt + validation error, NEVER the key; structured error after exhaustion; do not surface a malformed body) and the `validateProposedBody` rejection list (empty, wrapped in ``` fences, frontmatter mangled). RESEARCH §"Common Pitfalls → Pitfall 6" (over-eager churn; reject fenced/empty/frontmatter-mangled). PATTERNS.md §"internal/agent/propose.go" — round-trip golden idiom from internal/okf (frontmatter preserved; only Doc.Body changes). okf.Parse/Emit at internal/okf/okf.go.
  </read_first>
  <behavior>
    - validateProposedBody("") → error (empty).
    - validateProposedBody on a whole-body ```-fenced string → error (fenced).
    - validateProposedBody on a body whose YAML frontmatter keys were dropped/reordered relative to the original → error (frontmatter mangled).
    - validateProposedBody on a clean body that preserves frontmatter and only changes the intended span → nil.
    - the retry wrapper returns the structured "could not produce a valid patch" error after the attempt budget is exhausted, never a malformed body.
  </behavior>
  <action>
    Create `internal/agent/propose.go` with `validateProposedBody` (rejects empty, whole-body ```-fence wrapping, and frontmatter key-set/order changes relative to the source — use okf.Parse to compare frontmatter keys, never a regex on the raw bytes) and a `proposeWithRetry`-style wrapper (≤2 retries, log each failed attempt with attempt index + validation error but NEVER the API key, return a structured error after exhaustion). These are pure/deterministic and unit-testable without a key. Note: do NOT place the literal fence or frontmatter-mangling sample strings as prose the validator later negative-greps for — keep test fixtures inside the test file only. Do NOT inline implementation in this action; follow the AI-SPEC §4b shape.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && go build ./internal/agent/ && go test ./internal/agent/ -run TestValidateProposedBody -count=1 && go vet ./internal/agent/ && echo OK</automated>
  </verify>
  <done>validateProposedBody rejects empty/fenced/frontmatter-mangled bodies and accepts a clean frontmatter-preserving body; the retry wrapper exhausts to a structured error and never returns a malformed body; all tests run key-free.</done>
</task>

<task type="auto">
  <name>Task 2: Single-shot Summarize / Rewrite / Draft dispatch + prompts</name>
  <files>internal/agent/agent.go, internal/agent/prompts.go</files>
  <read_first>
    AI-SPEC §4 "Core Pattern (hybrid)" table — Summarize/Rewrite/Draft/Propose use single-shot ChatModel.Generate (context supplied directly, cheaper, fewer failure modes); the single-shot code example (§4) shows the context.WithTimeout(60s) + system+user message shape. §4 "Model Configuration" — Temperature 0.2 for Summarize, 0.4 acceptable for Draft; MaxTokens sized per mode (never nil); map-reduce/truncate if a body exceeds budget (Summarize). prompts.go from slices 2-3 (extend with summarize/rewrite/draft system prompts). PATTERNS.md §"internal/agent/agent.go" (lines 31-50: the single-shot path uses s.cm directly, not the ReAct agent; deps held as INTERFACES for testability). To make the dispatch unit-testable key-free (Task 4), the `cm` field must be typed as the eino `model` interface (`model.ToolCallingChatModel`, which embeds `model.ChatModel` and exposes `Generate(ctx, []*schema.Message, ...) (*schema.Message, error)`) — NOT the concrete `*openai.ChatModel` — so a fake can be injected. `openai.NewChatModel` already returns a value satisfying this interface, so no production wiring changes beyond the field type.
  </read_first>
  <action>
    In `internal/agent/agent.go`, add single-shot methods for Summarize (page + attachment), Rewrite (selection), and Draft (new page), each calling `s.cm.Generate` directly (NOT the ReAct agent) wrapped in context.WithTimeout(60s) with an explicit per-mode MaxTokens and Temperature (0.2 grounded; 0.4 for Draft). Summarize page/attachment pull the body/extracted text from the page/attachment services and truncate or map-reduce if over budget. Rewrite takes the selection text and returns the rewritten span (the server will diff old-selection↔new in slice 5/6 — this slice returns the proposal text, never auto-applies). Draft returns a full Markdown body (opens in the editor pending explicit save — never auto-written). Run Rewrite and Draft outputs through the validateProposedBody + retry harness from Task 1. Add the summarize/rewrite/draft system prompts to prompts.go (terse; "change only what is asked / return only the body, no fences" for rewrite; grounded summary for summarize).
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && go build ./internal/agent/ && go vet ./internal/agent/ && grep -q 'Generate' internal/agent/agent.go && echo OK</automated>
  </verify>
  <done>Summarize/Rewrite/Draft each use single-shot ChatModel.Generate with a 60s timeout + MaxTokens; Rewrite/Draft outputs pass validateProposedBody + retry; Draft opens in the editor pending save and Rewrite returns a proposal — neither auto-writes.</done>
</task>

<task type="auto">
  <name>Task 3: /summarize-page + /summarize-attachment endpoints + rewrite/draft wiring</name>
  <files>internal/server/handlers_agent.go, internal/server/router.go</files>
  <read_first>
    PATTERNS.md §"internal/server/handlers_agent.go" (handler skeleton + error mapping + writeJSON/writeError + actorUsername) and §"internal/server/router.go" — summarize/rewrite/draft are reads/proposals: summarize-page/attachment can be any-authed (the authed group); rewrite/draft produce proposals (editor-relevant but no write yet — keep consistent with the propose gating decided in slice 5). The slice-2/3 handler request decode; extend it. Audit via ActionAgentPrompt (non-fatal), scope in non-secret Detail.
  </read_first>
  <action>
    In `internal/server/handlers_agent.go`, add `/summarize-page` and `/summarize-attachment` handlers (decode the target, call the single-shot Summarize methods, return JSON — these return whole answers, not streams, per the deferred-ideas decision that only Ask/chat streams). Wire rewrite + draft dispatch (return the proposal/draft body as JSON for the frontend to route to the diff dialog or editor). Mount summarize-page/attachment in the authed group in router.go; keep ActionAgentPrompt auditing with the mode/scope in Detail (no secrets). Fail-closed on agent-off/unreachable.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && go build ./... && go vet ./internal/server/ ./internal/agent/ && grep -q 'summarize' internal/server/router.go && echo OK</automated>
  </verify>
  <done>The binary builds and vets; `/summarize-page` and `/summarize-attachment` return grounded summaries as JSON; rewrite/draft dispatch returns proposal/draft bodies; routes are mounted and audited; agent-off fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Key-free dispatch test (per-mode path + validate + timeout, fake model)</name>
  <files>internal/agent/dispatch_test.go</files>
  <read_first>
    PATTERNS.md §"internal/agent/agent.go" (lines 31-50: service holds deps as INTERFACES so unit tests inject fakes without standing up the provider; `cm` is the eino `model.ToolCallingChatModel` per Task 2) and §"internal/agent/tools_test.go" (line 62-64: the test idiom analog is `internal/audit/audit_test.go` — table-driven, key-free, runs offline with NO API key). The eino `model.ToolCallingChatModel` interface (embeds `model.ChatModel`: `Generate(ctx, []*schema.Message, ...model.Option) (*schema.Message, error)`) — a fake records the request messages + returns a canned `*schema.Message`, no network. validateProposedBody from Task 1 (the function Rewrite/Draft outputs must pass before return). The single-shot timeout shape (context.WithTimeout(60s)) from Task 2 / AI-SPEC §4.
  </read_first>
  <behavior>
    - Summarize routes to the summarize single-shot path (the fake's recorded system prompt / mode matches Summarize), NOT Rewrite/Draft.
    - Rewrite routes to the rewrite path AND its output passes through validateProposedBody before return (a fake returning a ```-fenced body makes Rewrite error/retry, never returns the fenced body).
    - Draft routes to the draft path AND its output passes through validateProposedBody before return (same fenced-body rejection).
    - Each dispatch sets a context with a deadline ~60s out (assert the ctx handed to the fake's Generate has a deadline within an expected window).
    - All cases run with DEEPSEEK_API_KEY unset (fake model, no network).
  </behavior>
  <action>
    Create `internal/agent/dispatch_test.go` with a fake satisfying the eino `model.ToolCallingChatModel` interface (records the messages + the ctx it was called with, returns a canned `*schema.Message`; a variant returns a ```-fenced body to exercise the validate/retry path). Build an `agent.Service` with the fake `cm` injected (and narrow fakes for the page/attachment readers Summarize needs). Table-driven (mode → expected path), asserting per the behavior block: correct single-shot path per mode (Summarize vs Rewrite vs Draft), Rewrite and Draft outputs pass through validateProposedBody (a fenced/empty fake body is rejected, never returned), and the ctx passed to Generate carries a ~60s deadline. Name the top-level test `TestDispatch`. The test MUST run key-free (no DEEPSEEK_API_KEY, no network) — mirror the `internal/audit/audit_test.go` offline idiom. Do not inline implementation here; follow the cited analogs.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && env -u DEEPSEEK_API_KEY go test ./internal/agent/ -run TestDispatch -count=1 && go vet ./internal/agent/ && echo OK</automated>
  </verify>
  <done>`TestDispatch` runs with DEEPSEEK_API_KEY unset (fake model, no network) and asserts: the correct single-shot path per mode (Summarize/Rewrite/Draft), Rewrite/Draft outputs pass validateProposedBody (fenced/empty rejected), and the ~60s timeout context is set on the Generate call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model free-text output → user/file | Rewrite/Draft/Summarize produce free text; a body that will later be applied must be validated before it can reach a file. |
| page/attachment content → single-shot prompt | untrusted content supplied directly in the user turn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering (data integrity) | Rewrite/Draft body output | mitigate | validateProposedBody rejects fenced/empty/frontmatter-mangled bodies; ≤2 retries then a structured error — a malformed body never reaches the user/diff. The key-free `TestDispatch` (Task 4) proves Rewrite/Draft outputs pass validateProposedBody before return. AGNT-07/08; AI-SPEC §1 #4 + Pitfall 6. |
| T-04-13 | Tampering | indirect injection via summarized content | mitigate | structural: no write tool, Draft/Rewrite never auto-write (Draft → editor pending save; Rewrite → proposal); untrusted content delimited in the user turn. AI-SPEC §1 #3. |
| T-04-14 | Denial of Service | large-body Summarize | mitigate | truncate/map-reduce over budget; context.WithTimeout(60s) + MaxTokens cap. AI-SPEC §4 Context Window Strategy. |
| T-04-15 | Information Disclosure | summarize/rewrite over role-scoped content | mitigate | page/attachment fetched via repo.Resolve-backed services honoring the session role; config/env/session never in the prompt. AGNT-11; V4. |
</threat_model>

<verification>
- `go build ./...` + `go vet` green; TestValidateProposedBody green; TestDispatch green (key-free, DEEPSEEK_API_KEY unset, fake model — asserts per-mode path + validateProposedBody + 60s timeout); TestToolSet still green (no new tool).
- grep confirms summarize routes + Generate path.
- Manual (VALIDATION.md): summarize a page/attachment, rewrite a selection (proposal returned), draft a page (opens in editor pending save).
</verification>

<success_criteria>
Summarize page/attachment, Rewrite selection, and Draft new page all work via single-shot Generate with timeout + token caps; Rewrite/Draft bodies pass validateProposedBody + retry and never auto-write; the summarize endpoints are mounted and audited; the read-only tool boundary is untouched.
</success_criteria>

<output>
Create `.planning/phases/04-eino-agent/04-04-SUMMARY.md` when done.
</output>
