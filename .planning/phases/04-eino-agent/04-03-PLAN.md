---
phase: 04-eino-agent
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - internal/agent/agent.go
  - internal/agent/prompts.go
  - internal/server/handlers_agent.go
autonomous: true
requirements: [AGNT-02, AGNT-03, AGNT-04]
must_haves:
  truths:
    - "A user can ask about selected text (the selection is passed in the user turn and scopes the answer)"
    - "A user can ask about a selected attachment (answered from its extracted text via read_attachment_text)"
    - "A user can ask about the whole workspace; the answer is search-backed RAG (top-K via search_pages/search_attachments), never a workspace dump"
    - "A workspace answer names the source page(s) it drew from (citation derived from the tool-call trace, role-scoped)"
    - "Workspace retrieval is scoped to the session role — out-of-role pages never enter the prompt or the answer"
  artifacts:
    - path: "internal/agent/agent.go"
      provides: "scope-aware Ask dispatch (page/selection/attachment/workspace) over the same ReAct agent"
      contains: "scope"
    - path: "internal/server/handlers_agent.go"
      provides: "/agent/chat accepts scope=selection|attachment|workspace + the citation line on workspace answers"
      contains: "workspace"
  key_links:
    - from: "internal/agent/agent.go"
      to: "internal/search/query.go"
      via: "workspace Ask uses search_pages/search_attachments tools → search.Index.Query top-K, role-scoped"
      pattern: "Query\\(ctx"
    - from: "internal/agent/agent.go"
      to: "internal/agent/tools.go"
      via: "attachment Ask drives read_attachment_text; selection passed in the user turn"
      pattern: "read_attachment_text"
  prohibitions:
    - "Whole-workspace Ask MUST NOT dump the entire workspace into the prompt — only top-K retrieved chunks"
    - "Retrieval MUST be filtered to role-readable pages from the server session, never trusting a client-supplied role/scope claim"
    - "config/env/session/other-users' private pages are never assembled into the prompt or surfaced in an answer"
    - "No new tool is added — the 5-tool allow-list (and TestToolSet) is unchanged; this slice only varies scope/prompting"
---

<objective>
Slice 3 of 6: expand Ask from page-only to all four scopes — selection, attachment, and whole-workspace (search-backed RAG) — over the SAME ReAct agent and read-only tool set built in slice 2. Add source attribution (D3) on workspace answers from the tool-call trace, and enforce role-scoped retrieval.

Purpose: AGNT-02 (selection), AGNT-03 (attachment), AGNT-04 (workspace RAG). After this slice a user can ask about anything in their role-readable workspace and get a grounded, cited answer without the agent ever dumping the workspace.
Output: scope-aware dispatch in agent.go, the workspace/attachment/selection system prompts in prompts.go, and the `/agent/chat` scope handling + citation line in handlers_agent.go. No new tool — the allow-list is unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-eino-agent/04-RESEARCH.md
@.planning/phases/04-eino-agent/04-AI-SPEC.md
@.planning/phases/04-eino-agent/04-PATTERNS.md
@.planning/phases/04-eino-agent/04-02-SUMMARY.md

# Existing seams (read cited ranges only)
@internal/search/query.go
@internal/pages/service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope-aware Ask dispatch (selection / attachment / workspace) + role-scoped RAG</name>
  <files>internal/agent/agent.go</files>
  <read_first>
    RESEARCH.md §"Phase Requirements" rows AGNT-02/03/04 and §"Context Window Strategy" / AI-SPEC §4 "Context Window Strategy" (workspace = top-K≈5 Bleve chunks via search_* tools, never dump; selection passed in the user turn; attachment via read_attachment_text). AI-SPEC §1b "Scope discipline & refusal" + §6 "Role-scoped context assembly" guardrail (workspace-scope reads honor the server session role, never the client; exclude config/env/session/other users' pages). PATTERNS.md §"internal/agent/tools.go" — search.Result carries Path+Snippet for citation (D3). Open Question #2 in RESEARCH: attach citations from the tool-call trace (paths actually retrieved), more reliable than trusting the model to cite.
  </read_first>
  <action>
    In `internal/agent/agent.go`, extend the Ask path to accept a scope (page | selection | attachment | workspace). For selection: pass the selected text in the user turn (delimited as untrusted) so the model answers about just that span. For attachment: the agent uses read_attachment_text over the selected attachment id (the extracted .txt). For workspace: rely on the ReAct loop calling search_pages/search_attachments (top-K≈5) — the retrieval services must be constructed role-scoped from the session (reader/editor from the server session, never a client-supplied claim) so out-of-role pages never enter the prompt. Capture the set of page paths the agent actually retrieved from the tool-call trace and return them alongside the answer for the citation line (D3). Do NOT add any new tool — the allow-list and TestToolSet stay unchanged; this is scope + prompting only. Ensure config/env/session/other-users' content is never assembled into the prompt.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && go build ./internal/agent/ && go test ./internal/agent/ -run TestToolSet -count=1 && go vet ./internal/agent/ && echo OK</automated>
  </verify>
  <done>Ask dispatches on scope; selection text scopes the answer; attachment Ask reads extracted text; workspace Ask retrieves top-K role-scoped chunks (no dump) and returns the retrieved page paths for citation; the 5-tool allow-list is unchanged (TestToolSet still green).</done>
</task>

<task type="auto">
  <name>Task 2: Per-scope system prompts + /agent/chat scope handling + workspace citation line</name>
  <files>internal/agent/prompts.go, internal/server/handlers_agent.go</files>
  <read_first>
    AI-SPEC §4b "Prompt Engineering Discipline" (per-mode system prompts in prompts.go; untrusted content delimited in the USER turn) and §1b "Source attribution" dimension (a workspace answer names the page(s) so the reader can verify). UI-SPEC "AgentPanel States → Done" — the muted citation line "Reasoned over: [page-a](…), [page-b](…)" rendered from retrieved paths. PATTERNS.md §"internal/server/handlers_agent.go" (the slice-2 handler; extend its request decode for scope). prompts.go from slice 2 (extend, do not rewrite the Ask-page prompt).
  </read_first>
  <action>
    Add the selection / attachment / workspace system prompts to `internal/agent/prompts.go` (terse; each instructs the model to answer only from the supplied/retrieved context and to say "that isn't in these pages" rather than fabricate — D7 refusal). In `internal/server/handlers_agent.go`, extend the `/agent/chat` request to carry scope (selection text / attachment id / workspace flag) and pass it to the scope-aware dispatch; on workspace answers, emit the retrieved page paths so the frontend can render the "Reasoned over:" citation line (D3). Keep the ActionAgentPrompt audit (record the scope in a non-secret Detail, e.g. "scope=workspace"). The handler stays fail-closed on agent-off/unreachable.
  </action>
  <verify>
    <automated>cd /home/john/go/src/github.com/postfix/okworkspace && go build ./... && go vet ./internal/server/ ./internal/agent/ && grep -q 'workspace' internal/agent/prompts.go && echo OK</automated>
  </verify>
  <done>The binary builds and vets; `/agent/chat` accepts all four scopes; selection/attachment/workspace prompts instruct grounded-or-refuse behavior; workspace answers carry retrievable page citations; the prompt audit records the scope without secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client scope claim → retrieval | the client says which scope; the ROLE that bounds retrieval must come from the server session, not the client. |
| workspace content → answer | RAG can pull from many pages; only role-readable pages may enter the prompt/answer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | workspace RAG retrieval | mitigate | search_* tools constructed role-scoped from the server session (never the client); out-of-role pages never enter the prompt or answer. AGNT-04; AI-SPEC §1 #2 + §1b "scope leak"; V4. |
| T-04-09 | Information Disclosure | answer assembly | mitigate | config/env/session/other-users' private data is never assembled into the prompt by construction; D6/D7 adversarial eval cases (slice 5 eval set) cover leak attempts. AGNT-11; AI-SPEC §6. |
| T-04-10 | Tampering | indirect injection via attachment/page content (selection/attachment/workspace) | mitigate | structural write boundary still holds (no write tool reachable); untrusted content delimited in the USER turn; answer-only, no apply path in this slice. AI-SPEC §1 #3. |
| T-04-11 | Denial of Service | workspace RAG token blow-up | mitigate | top-K≈5 retrieval, never dump the workspace; MaxStep≤12 bounds the ReAct accumulation; 60s timeout. AI-SPEC §4 Context Window Strategy. |
</threat_model>

<verification>
- `go build ./...` + `go vet` green; TestToolSet still passes (no new tool).
- grep confirms workspace prompt exists.
- Manual (VALIDATION.md): Ask about a selection, an attachment, and the whole workspace each return a scoped, grounded answer; a workspace answer shows a "Reasoned over:" citation.
</verification>

<success_criteria>
All four Ask scopes work over the unchanged read-only tool set; workspace Ask is top-K role-scoped RAG (no dump, no out-of-role leak) with tool-trace-derived citations; selection and attachment Ask are correctly scoped; the allow-list and its build gate are untouched.
</success_criteria>

<output>
Create `.planning/phases/04-eino-agent/04-03-SUMMARY.md` when done.
</output>
