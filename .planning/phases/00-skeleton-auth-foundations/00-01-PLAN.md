---
phase: 00-skeleton-auth-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/okf-workspace/main.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/store/db.go
  - internal/store/migrations.go
  - internal/store/migrations/0001_init.sql
  - internal/store/store_test.go
  - internal/auth/password.go
  - internal/auth/password_test.go
  - internal/auth/session.go
  - internal/auth/auth.go
  - internal/auth/auth_test.go
  - internal/users/users.go
  - internal/users/bootstrap.go
  - internal/users/bootstrap_test.go
  - internal/server/router.go
  - internal/server/middleware.go
  - internal/server/handlers_auth.go
  - internal/server/handlers_auth_test.go
  - internal/web/embed.go
  - web/package.json
  - web/vite.config.ts
  - web/tsconfig.json
  - web/index.html
  - web/eslint.config.js
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/api/client.ts
  - web/src/routes/Login.tsx
  - web/src/routes/AppShell.tsx
  - web/src/styles/tokens.css
  - web/dist/.gitkeep
  - config.example.yaml
  - .gitignore
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-04, AUTH-06, SEC-03, SEC-04]
must_haves:
  truths:
    - "Running the single binary with no users prints a one-time admin password to the log and creates the admin user"
    - "A user can submit username + password on the login screen and, with valid credentials, land on the AppShell showing their display name"
    - "After login the session persists across a browser refresh via an HTTPOnly, SameSite cookie"
    - "Invalid credentials return a generic error and never reveal whether the username exists"
    - "Mutating auth requests carry a CSRF token; requests without a valid token are rejected"
  artifacts:
    - path: "cmd/okf-workspace/main.go"
      provides: "serve command + DI wiring + startup sequence (config -> store -> bootstrap -> http server)"
      contains: "serve"
    - path: "internal/auth/password.go"
      provides: "Argon2id hash + constant-time verify"
      contains: "argon2id"
    - path: "internal/auth/session.go"
      provides: "SCS session manager with SQLite store and HTTPOnly/SameSite cookie config"
      contains: "SameSite"
    - path: "internal/store/migrations/0001_init.sql"
      provides: "users and sessions tables"
      contains: "users"
    - path: "internal/server/handlers_auth.go"
      provides: "login/logout/me handlers"
      contains: "login"
    - path: "web/src/routes/Login.tsx"
      provides: "login form wired to POST /api/v1/auth/login"
      min_lines: 30
    - path: "web/src/routes/AppShell.tsx"
      provides: "authenticated shell rendering the user's display name"
      min_lines: 20
  key_links:
    - from: "web/src/routes/Login.tsx"
      to: "/api/v1/auth/login"
      via: "fetch POST with CSRF header"
      pattern: "auth/login"
    - from: "internal/server/handlers_auth.go"
      to: "internal/auth"
      via: "Authenticate + session create"
      pattern: "auth\\."
    - from: "internal/auth/auth.go"
      to: "internal/store"
      via: "user lookup by username"
      pattern: "users"
---

<objective>
Deliver the Walking Skeleton: the thinnest end-to-end working stack that proves a non-technical user can sign in to the single-binary OKF Workspace. Scaffold the Go module + cobra `serve` command + config loader + shared SQLite store, build the real auth spine (Argon2id hashing, SCS server-side sessions, nosurf CSRF, admin bootstrap), and wire a real React login form → `POST /api/v1/auth/login` → Argon2id verify against the SQLite `users` table → session cookie → authenticated AppShell render.

Purpose: Establish the architectural backbone (per 00-SKELETON.md) that every later plan and phase builds on, and satisfy the core Phase-0 success criterion "a user can log in and see their display name." This is the load-bearing scaffold — get the directory layout, DI wiring, startup order, and the cookie/CSRF model right here so later plans slot in.
Output: A buildable single binary that, on first run, bootstraps an admin user (one-time password to the log) and serves a working login → AppShell flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-skeleton-auth-foundations/00-CONTEXT.md
@.planning/phases/00-skeleton-auth-foundations/00-SKELETON.md
@.planning/phases/00-skeleton-auth-foundations/00-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@CLAUDE.md
@SPEC.md
</context>

<artifacts_this_phase_produces>
This task introduces NEW symbols that do not yet exist in the (greenfield) repo. The source-grounding pass MUST treat these as newly created, not drift:

**Go module / commands**
- Go module path: `github.com/postfix/okworkspace` (set in `go.mod`)
- CLI: `okf-workspace` binary; cobra root command + `serve --config <path>` subcommand
- `cmd/okf-workspace/main.go` — `main()`, `serve` command, DI wiring, startup sequence

**Go packages / exported symbols (created here)**
- `internal/config`: `Config` struct, `Load(path string) (Config, error)`; config keys consumed: `server.listen`, `server.public_url`, `storage.data_dir`, `auth.session_cookie_name`, `auth.session_ttl_hours`, admin username key (default `admin`, D-03)
- `internal/store`: `Open(dsn string) (*Store, error)`, `*Store` wrapping `*sql.DB`, `Migrate(ctx) error`
- `internal/auth`: `HashPassword(plain string) (string, error)`, `VerifyPassword(hash, plain string) (bool, error)`, `NewSessionManager(store, cfg) *scs.SessionManager`, `Authenticate(ctx, username, plain string) (*users.User, error)`
- `internal/users`: `User` struct (fields: ID, Username, DisplayName, Role, PasswordHash, MustChangePassword, Active), `GetByUsername(ctx, username) (*User, error)`, `BootstrapAdmin(ctx, cfg) (username string, generatedPassword string, created bool, err error)`
- `internal/server`: `New(deps) http.Handler`, middleware stack (recover, request-id, session, CSRF), auth handlers
- `internal/web`: `Handler() http.Handler` (embed.FS of `web/dist` + SPA fallback)

**HTTP routes (created here)**
- `POST /api/v1/auth/login`, `POST /api/v1/auth/logout`, `GET /api/v1/auth/me`
- `GET /api/v1/csrf` (or equivalent) — delivers the CSRF token to the SPA
- SPA static + history-fallback served at `/`

**SQLite tables (created here)**
- `users` (id, username UNIQUE, display_name, role, password_hash, must_change_password, active, created_at)
- `sessions` (SCS SQLite-store schema)

**Config keys (created here)**
- `config.yaml` keys per SPEC §20.3: `server.*`, `storage.*`, `auth.session_cookie_name`, `auth.session_ttl_hours`

**React routes / components (created here)**
- routes `/login`, `/app`; components `Login`, `AppShell`; `api/client.ts`; `styles/tokens.css`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Go module, config loader, and shared SQLite store</name>
  <files>go.mod, go.sum, cmd/okf-workspace/main.go, internal/config/config.go, internal/config/config_test.go, internal/store/db.go, internal/store/migrations.go, internal/store/migrations/0001_init.sql, internal/store/store_test.go, config.example.yaml, .gitignore, web/dist/.gitkeep</files>
  <read_first>
    - CLAUDE.md (LOCKED stack: Go 1.26, modernc.org/sqlite, cobra, yaml.v3, slog, CGO_ENABLED=0 — and the "What NOT to Use" table: no go-sqlite3 cgo driver, no storing content in SQLite)
    - SPEC.md §16 + §16.1 (package layout, service responsibilities), §20.2 (data directory), §20.3 (config.yaml schema)
    - .planning/research/ARCHITECTURE.md (Component Responsibilities table; "internal/store" rationale — one shared *sql.DB + migrations; Build Order item 1)
    - .planning/phases/00-skeleton-auth-foundations/00-SKELETON.md (Architectural Decisions table)
  </read_first>
  <behavior>
    - config_test: `Load` on a valid config.yaml returns the typed Config with server.listen, storage.data_dir, auth.session_cookie_name (default "okf_session"), auth.session_ttl_hours (default 168), and admin username (default "admin" per D-03).
    - config_test: `Load` applies defaults when optional keys are omitted, and an env override for the admin username takes precedence over the file default.
    - store_test: `Open` on a temp file DSN returns a Store; `Migrate` is idempotent (running it twice succeeds and leaves the `users` and `sessions` tables present).
    - store_test: after `Migrate`, a row can be inserted into `users` and read back (real write + read).
  </behavior>
  <action>
    Initialize the Go module as `github.com/postfix/okworkspace` (go 1.26) and add the LOCKED deps: `github.com/spf13/cobra` v1.10.2, `modernc.org/sqlite` v1.52.0, `gopkg.in/yaml.v3` v3.0.1. Build with `CGO_ENABLED=0`.

    Create `internal/config/config.go`: a typed `Config` struct mirroring SPEC §20.3 (server.listen, server.public_url, storage.data_dir, storage.repo_dir, git.*, auth.session_cookie_name, auth.session_ttl_hours; agent/search/attachments keys may be parsed-but-unused placeholders this phase). Add an admin section with a configurable username (default `admin`, per D-03). `Load(path string) (Config, error)` reads YAML, applies defaults (session_cookie_name=`okf_session`, session_ttl_hours=168, admin username=`admin`), and supports env overrides for the admin username. Write `config.example.yaml` matching SPEC §20.3 and add `config.yaml`, `web/node_modules`, and the data dir to `.gitignore` (commit `web/dist/.gitkeep` so the embed target exists).

    Create `internal/store/db.go` with `Open(dsn string) (*Store, error)` returning a `*Store` that wraps a single shared `*sql.DB` opened via the pure-Go `modernc.org/sqlite` driver (driver name `sqlite`), with `PRAGMA foreign_keys=ON` and WAL mode. Create `internal/store/migrations.go` + `internal/store/migrations/0001_init.sql` embedded via `embed.FS`; `(*Store).Migrate(ctx)` applies pending migrations idempotently (a `schema_migrations` table tracks applied versions). The `0001_init.sql` creates the `users` table (id INTEGER PK, username TEXT UNIQUE NOT NULL, display_name TEXT NOT NULL, role TEXT NOT NULL, password_hash TEXT NOT NULL, must_change_password INTEGER NOT NULL DEFAULT 0, active INTEGER NOT NULL DEFAULT 1, created_at TEXT NOT NULL) and the `sessions` table required by the SCS SQLite store (token TEXT PK, data BLOB NOT NULL, expiry REAL NOT NULL, with an index on expiry). SQLite is operational-data ONLY — never wiki content.

    Create `cmd/okf-workspace/main.go` with a cobra root command and a `serve --config <path>` subcommand whose runtime (in this task) loads config, opens the store, and runs migrations, logging via `log/slog` (JSON handler). Full server wiring is added in Task 2/3.
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go test ./internal/config/... ./internal/store/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: `go.mod` declares module `github.com/postfix/okworkspace` and go `1.26`.
    - CLI: `CGO_ENABLED=0 go build ./...` exits 0 (single static binary, no cgo).
    - Source: `internal/store/db.go` imports `modernc.org/sqlite` (NOT `github.com/mattn/go-sqlite3`).
    - Behavior: `go test ./internal/store/...` passes including the idempotent-migrate and insert-then-read assertions.
    - Behavior: `go test ./internal/config/...` passes including default-application and env-override assertions.
    - Source: `internal/store/migrations/0001_init.sql` contains `users` and `sessions` table definitions; no table stores Markdown/page content.
  </acceptance_criteria>
  <done>Module builds with CGO disabled; config loads with defaults + env override; SQLite store migrates idempotently and supports a real write+read on `users`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth spine — Argon2id, SCS sessions, CSRF, admin bootstrap, login/logout/me API</name>
  <files>internal/auth/password.go, internal/auth/password_test.go, internal/auth/session.go, internal/auth/auth.go, internal/auth/auth_test.go, internal/users/users.go, internal/users/bootstrap.go, internal/users/bootstrap_test.go, internal/server/router.go, internal/server/middleware.go, internal/server/handlers_auth.go, internal/server/handlers_auth_test.go, cmd/okf-workspace/main.go</files>
  <read_first>
    - internal/store/db.go and internal/store/migrations/0001_init.sql (from Task 1 — the users/sessions schema this task reads/writes)
    - internal/config/config.go (from Task 1 — session cookie name/TTL, admin username)
    - SPEC.md §6.5 (user management), §17.1 (auth API shape: POST /api/v1/auth/login, /logout, GET /auth/me + me response {username, display_name, role}), §21.4 (Argon2id, HTTPOnly, SameSite, CSRF, admin bootstrap)
    - .planning/phases/00-skeleton-auth-foundations/00-CONTEXT.md (D-01 strong random password printed once; D-03 configurable admin username; D-04 noted but CLI reset is Plan 03; Claude's Discretion: SameSite=Lax, cookie okf_session, TTL 168h, SCS SQLite store, nosurf double-submit on ALL mutating routes, generic "invalid credentials" error)
    - .planning/research/PITFALLS.md (Security Mistakes table: CSRF on ALL mutating routes; admin-bootstrap weak/default password forbidden; generic login error / no account enumeration)
    - CLAUDE.md (alexedwards/argon2id, alexedwards/scs/v2 + SQLite store, justinas/nosurf — and "What NOT to Use": bcrypt forbidden as default)
  </read_first>
  <behavior>
    - password_test: `HashPassword` returns a PHC-format Argon2id string (prefix `$argon2id$`); `VerifyPassword(hash, correct)` is true; `VerifyPassword(hash, wrong)` is false; a plaintext password never appears in the returned hash.
    - bootstrap_test: `BootstrapAdmin` on an empty `users` table creates exactly one admin row with `must_change_password=1`, returns the generated plaintext once, and the stored `password_hash` verifies against that plaintext; running it again when a user already exists is a no-op (created=false, no new row, no new password printed).
    - auth_test: `Authenticate` with the bootstrap username + generated password returns the user; with a wrong password returns an error; with a non-existent username returns the SAME generic error (no enumeration) and still performs a hash comparison to avoid timing leaks.
    - handlers_auth_test: `POST /api/v1/auth/login` with valid creds returns 200 and a `Set-Cookie` header for `okf_session` with `HttpOnly` and `SameSite=Lax`; with invalid creds returns 401 and the generic message; `GET /api/v1/auth/me` after login returns {username, display_name, role}; a mutating request (login) without a valid CSRF token is rejected.
  </behavior>
  <action>
    Add LOCKED deps: `github.com/alexedwards/argon2id` v1.0.0, `github.com/alexedwards/scs/v2` v2.9.0 (+ its sqlite3 store; use the modernc-compatible store wiring), `github.com/justinas/nosurf` v1.2.0, `github.com/go-chi/chi/v5` v5.3.0.

    `internal/auth/password.go`: `HashPassword(plain) (string, error)` and `VerifyPassword(hash, plain) (bool, error)` thin wrappers over `argon2id.CreateHash`/`argon2id.ComparePasswordAndHash` (constant-time, PHC format). Never log plaintext. bcrypt is forbidden as the default.

    `internal/auth/session.go`: `NewSessionManager(store, cfg) *scs.SessionManager` configured to use the SCS SQLite store backed by the shared `*sql.DB` (sessions persist in `app.db`). Cookie: `Name=cfg.Auth.SessionCookieName` (default `okf_session`), `HttpOnly=true`, `SameSite=http.SameSiteLaxMode`, `Secure` set when `server.public_url` is https, `Lifetime=session_ttl_hours`.

    `internal/users/users.go`: `User` struct + `GetByUsername(ctx, username) (*User, error)`. `internal/users/bootstrap.go`: `BootstrapAdmin(ctx, cfg)` runs ONLY when no users exist — generate a strong random password (crypto/rand, >= 24 chars), hash with Argon2id, insert the admin row with the configured username (D-03) and `must_change_password=1` (D-02 flag persisted now; enforcement UI is Plan 03), then print the plaintext exactly once to the log with a clear "save this, it will not be shown again" message (D-01). Never ship a fixed default password.

    `internal/auth/auth.go`: `Authenticate(ctx, username, plain)` looks up the user via `users.GetByUsername`, verifies with Argon2id, returns a generic error for both unknown-user and wrong-password (no account enumeration); perform a dummy hash comparison on the unknown-user path to equalize timing.

    `internal/server/middleware.go`: middleware stack — chi `Recoverer`, request-id, the SCS `LoadAndSave` session middleware, and `nosurf` CSRF protection applied to ALL mutating routes (POST/PUT/DELETE). `internal/server/router.go`: `New(deps) http.Handler` mounting `/api/v1`. `internal/server/handlers_auth.go`: `POST /api/v1/auth/login` (reads {username, password}, authenticates, stores user id in the SCS session, renews the session token to prevent fixation), `POST /api/v1/auth/logout` (destroys the session), `GET /api/v1/auth/me` (returns {username, display_name, role} from the session-bound user, 401 if no session), and a `GET /api/v1/csrf` endpoint that returns the nosurf token so the SPA can echo it in a header. Wire all of this into the `serve` command in `cmd/okf-workspace/main.go` after migrations (startup order: config -> store -> migrate -> bootstrap admin -> build server -> listen).
  </action>
  <verify>
    <automated>CGO_ENABLED=0 go test ./internal/auth/... ./internal/users/... ./internal/server/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal/auth/password.go` imports `github.com/alexedwards/argon2id`; the repo contains NO use of `golang.org/x/crypto/bcrypt` as the default hasher (grep: `grep -rn "bcrypt" internal/ | grep -v '_test' | grep -vc '^#'` returns 0).
    - Behavior: `POST /api/v1/auth/login` with valid credentials returns 200 with `Set-Cookie: okf_session=...; HttpOnly; SameSite=Lax` (test asserts the header).
    - Behavior: `POST /api/v1/auth/login` with invalid credentials returns 401 and the exact generic message "Invalid username or password." with identical response for unknown-user vs wrong-password.
    - Behavior: bootstrap creates exactly one admin user on an empty DB, prints the password once, and is a no-op on a non-empty DB (test asserts row count and created=false).
    - Behavior: a login POST without a valid CSRF token is rejected by nosurf (test asserts non-200/403).
    - Source: `internal/auth/session.go` sets `HttpOnly` true and `SameSite` lax.
  </acceptance_criteria>
  <done>Argon2id hash/verify, SCS SQLite-backed sessions with HTTPOnly+SameSite cookie, nosurf CSRF on mutating routes, admin bootstrap (one-time password), and login/logout/me handlers all pass their tests and are wired into `serve`.</done>
</task>

<task type="auto">
  <name>Task 3: React scaffold + login -> AppShell vertical slice, embedded and runnable</name>
  <files>internal/web/embed.go, internal/server/router.go, web/package.json, web/vite.config.ts, web/tsconfig.json, web/index.html, web/eslint.config.js, web/src/main.tsx, web/src/App.tsx, web/src/api/client.ts, web/src/routes/Login.tsx, web/src/routes/AppShell.tsx, web/src/styles/tokens.css</files>
  <read_first>
    - internal/server/handlers_auth.go and internal/server/router.go (from Task 2 — the API contract this UI calls: /api/v1/auth/login, /auth/me, /auth/logout, /csrf)
    - internal/web/embed.go (target of the embed; create if absent)
    - .planning/phases/00-skeleton-auth-foundations/00-UI-SPEC.md (FULL — design tokens in web/src/styles/tokens.css; login card focal point + "Sign in" primary CTA; AppShell top bar with display name + Log out reachable from any page; copywriting contract incl. "Invalid username or password.", "Sign in to your workspace"; system font stack; lucide-react icons; no Tailwind/Radix/shadcn)
    - CLAUDE.md (LOCKED frontend stack: React 19.2.7, Vite 8.0.16, TS 6.0.3, @tanstack/react-query 5.101.0, zustand 5.0.14, react-router-dom 7.18.0, @vitejs/plugin-react 6.0.2, ESLint 10.5.0, lucide-react; embedded via go:embed)
    - SPEC.md §18.1 (routes /login, /app), §18.2 (AppShell, LoginForm components), §20.1 (single-binary build: cd web && npm run build, then go build)
  </read_first>
  <action>
    Scaffold the Vite + React 19 + TS 6 app under `web/` (D-11 builds the REAL AppShell, not a throwaway): `package.json` with the LOCKED versions (react/react-dom 19.2.7, vite 8.0.16, typescript 6.0.3, @vitejs/plugin-react 6.0.2, react-router-dom 7.18.0, @tanstack/react-query 5.101.0, zustand 5.0.14, lucide-react, eslint 10.5.0 flat config), `vite.config.ts` (build output to `web/dist`), `tsconfig.json`, `eslint.config.js` (flat config), `index.html`, `web/src/main.tsx` (mounts React Query provider + react-router-dom router).

    `web/src/styles/tokens.css`: declare ALL tokens from the UI-SPEC as CSS custom properties on `:root` (spacing `--space-*`, layout `--topbar-height`/`--navrail-width`/`--login-card-width`, typography `--font-size-*`/`--font-weight-*`/`--line-height-*`, color `--color-*`, radius `--radius-*`, shadow `--shadow-*`). Components reference `var(--…)` only — no hard-coded hex/px in component CSS. Use the system font stack; no web-font download.

    `web/src/api/client.ts`: a fetch wrapper that (a) reads the CSRF token from `GET /api/v1/csrf` and echoes it in the configured header on every mutating request, (b) sends credentials (cookies), (c) exposes `login(username, password)`, `me()`, `logout()`.

    `web/src/routes/Login.tsx`: the centered login card per UI-SPEC — wordmark "OKF Workspace" (28px/600 focal), subtitle "Sign in to your workspace", "Username"/"Password" labeled fields, single accent primary CTA "Sign in" (40px min hit height, loading spinner + disabled state, focus-visible ring). On submit, call `client.login`; on failure show the generic "Invalid username or password." On success navigate to `/app`.

    `web/src/routes/AppShell.tsx`: the authenticated shell per UI-SPEC + D-11 — persistent top bar (`--topbar-height`) showing the wordmark, the user's DISPLAY NAME (from `/auth/me`, AUTH-06), and a "Log out" control reachable from any page (calls `client.logout`, redirects to `/login`); a left nav rail (`--navrail-width`, secondary surface) rendering a static placeholder tree (read-only/disabled, muted — the real seeded tree wiring is Plan 02); and a main pane with the empty-state heading "Your workspace is ready" and body copy from the UI-SPEC. `web/src/App.tsx` defines routes: `/login` -> Login, `/app` -> AppShell (redirect to `/login` when `/auth/me` returns 401).

    `internal/web/embed.go`: `//go:embed all:dist` (the built SPA) exposed as `Handler() http.Handler` that serves static assets with a SPA history-fallback to `index.html` for non-`/api` paths; mount it as the catch-all in `internal/server/router.go`. Document the single-binary build/run in the SUMMARY: `cd web && npm install && npm run build` then `CGO_ENABLED=0 go build -o okf-workspace ./cmd/okf-workspace` then `./okf-workspace serve --config ./config.yaml`.
  </action>
  <verify>
    <automated>cd web && npm install && npm run build && npm run lint && cd .. && CGO_ENABLED=0 go build -o /tmp/okf-workspace ./cmd/okf-workspace && test -f web/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cd web && npm run build` produces `web/dist/index.html` and the bundled assets.
    - CLI: `npm run lint` (ESLint flat config) exits 0.
    - CLI: `CGO_ENABLED=0 go build -o /tmp/okf-workspace ./cmd/okf-workspace` exits 0 with the SPA embedded.
    - Source: `internal/web/embed.go` contains `//go:embed` of `dist` and serves a SPA history-fallback for non-`/api` routes.
    - Source: `web/src/styles/tokens.css` declares `--color-accent: #2563EB` and the `--space-*` scale; `web/src/routes/Login.tsx` references `var(--` tokens and contains the copy "Sign in to your workspace".
    - Source: `web/src/routes/AppShell.tsx` renders the display name from `/auth/me` and a "Log out" control; `web/src/api/client.ts` echoes the CSRF token on mutating requests.
    - Behavior (manual, captured in SUMMARY run-command): starting the binary with an empty DB prints a one-time admin password, and signing in with it on `/login` lands on `/app` showing the admin display name. (Automated coverage of the API path is in Task 2; this is the end-to-end UI confirmation.)
  </acceptance_criteria>
  <done>The Vite/React app builds and lints, embeds into the Go binary, and the login -> AppShell flow is wired to the real auth API; the single binary runs the full stack with the documented commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (SPA) -> Go HTTP API | Untrusted client input (login credentials, CSRF token, cookies) crosses here |
| HTTP handler -> SQLite store | User-controlled username reaches a parameterized query |
| Server process -> server log / stdout | The one-time bootstrap admin password is emitted here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00.01-01 | Spoofing | `POST /api/v1/auth/login` | mitigate | Argon2id `ComparePasswordAndHash` (constant-time) in `internal/auth`; SEC-03. No bcrypt default. |
| T-00.01-02 | Information disclosure | login error path | mitigate | Generic "Invalid username or password." for both unknown-user and wrong-password; dummy hash compare on unknown-user to equalize timing (no account enumeration). |
| T-00.01-03 | Tampering / CSRF | all mutating auth routes | mitigate | nosurf double-submit token on ALL POST/PUT/DELETE routes; SPA fetches token from `/api/v1/csrf` and echoes it in a header; SEC-04. |
| T-00.01-04 | Spoofing / session theft | session cookie | mitigate | SCS cookie HttpOnly + SameSite=Lax + Secure-when-TLS; session token renewed on login (anti session-fixation); AUTH-03 / SEC-04. |
| T-00.01-05 | Elevation of privilege | admin bootstrap | mitigate | Strong random (crypto/rand >=24 char) one-time password, Argon2id-hashed, printed once; `must_change_password=1` set (enforced in Plan 03); never a fixed default; D-01/D-02. |
| T-00.01-06 | Information disclosure | server log / stdout | mitigate | Only the one-time bootstrap password is logged (by design, D-01); plaintext passwords are NEVER logged on login/verify paths; slog handler emits no credential fields. |
| T-00.01-07 | Tampering / SQLi | username -> SQLite lookup | mitigate | All queries parameterized (`?` placeholders via database/sql); no string concatenation of user input. |
| T-00.01-SC | Tampering (supply chain) | go.mod / npm installs | mitigate | All packages are LOCKED in CLAUDE.md with validated versions from proxy.golang.org / npm (HIGH confidence in STACK.md). No `[ASSUMED]`/`[SUS]` packages introduced; if any new package is needed beyond the locked list, it requires a blocking legitimacy checkpoint before install. |
</threat_model>

<verification>
- `CGO_ENABLED=0 go build ./...` exits 0 (single static binary, no cgo toolchain).
- `CGO_ENABLED=0 go test ./... -count=1` passes for config, store, auth, users, server packages.
- `cd web && npm install && npm run build && npm run lint` exits 0; `web/dist/index.html` exists.
- Manual end-to-end (recorded in SUMMARY): empty-DB first run prints a one-time admin password; signing in with it lands on `/app` showing the admin display name; refresh keeps the session (cookie persists).
- `grep -rn "mattn/go-sqlite3\|crypto/bcrypt" internal/ cmd/ | grep -vc '^#'` returns 0 (forbidden libs absent).
</verification>

<success_criteria>
- A user can submit username + password and, with valid credentials, reach the AppShell showing their display name (AUTH-01, AUTH-06).
- The session persists across refresh via an HTTPOnly, SameSite cookie (AUTH-03, SEC-04).
- Mutating auth requests are CSRF-protected (SEC-04).
- First startup creates the admin user with a one-time logged password (AUTH-04, partial of success-criterion 3 — repo/Git init lands in Plan 02).
- Passwords are Argon2id-hashed (SEC-03).
- The single binary builds with CGO disabled and serves the embedded SPA + REST API from one process.
</success_criteria>

<output>
Create `.planning/phases/00-skeleton-auth-foundations/00-01-SUMMARY.md` when done, including the exact single-binary build + run commands and the observed one-time-admin-password first-run flow.
</output>
