---
phase: 02-attachments-text-extraction
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - internal/server/handlers_attachments.go
  - internal/server/handlers_attachments_test.go
  - web/src/api/client.ts
  - web/src/components/attachments/AttachmentCard.tsx
  - web/src/components/attachments/AttachmentCard.css
  - web/src/components/attachments/ImagePreviewDialog.tsx
  - web/src/components/attachments/AttachmentsSection.tsx
autonomous: true
requirements: [ATT-03, ATT-04]
must_haves:
  truths:
    - "Each attachment shows a card with the original filename (emphasised), a meta line of human-readable size · uploader · date, and a 64×64 thumbnail for images or a type icon for other files (ATT-03)"
    - "Clicking an image attachment's thumbnail opens a focus-trapped full-size preview dialog; the image is shown via <img src> only, so an uploaded SVG cannot execute script (ATT-04, stored-XSS guard)"
    - "The date and size are shown human-friendly (e.g. '1.4 MB', '21 Jun 2026'); the opaque on-disk id and any Git/SHA vocabulary never appear"
  artifacts:
    - path: "web/src/components/attachments/AttachmentCard.tsx"
      provides: "Full card: thumbnail/type-icon, filename, size·uploader·date meta line, download, image preview trigger"
      min_lines: 50
    - path: "web/src/components/attachments/ImagePreviewDialog.tsx"
      provides: "Focus-trapped full-size image preview via existing Dialog, <img src> only"
    - path: "internal/server/handlers_attachments.go"
      provides: "Inline-image download branch verified for png/jpg/svg (ATT-04)"
  key_links:
    - from: "web/src/components/attachments/AttachmentCard.tsx"
      to: "/api/v1/attachments/{id}/download"
      via: "<img src> thumbnail + Download link"
      pattern: "download"
    - from: "web/src/components/attachments/AttachmentCard.tsx"
      to: "ImagePreviewDialog"
      via: "thumbnail click opens preview"
      pattern: "ImagePreviewDialog"
---

## Phase Goal

**As an** internal team member, **I want to** see a clear attachment card with the file's name, size, uploader and date and preview images inline, **so that** I can recognise and open attachments without downloading them first.

> Slice 2 of 4. Builds on 02-01 (upload/download exist). After this plan, attachments render as full UI-SPEC cards and images preview inline. Slice 02-03 adds text-extraction status; 02-04 adds replace/remove/orphan-delete.

<objective>
Turn the minimal filename card from 02-01 into the full ATT-03 attachment card (thumbnail or type icon, emphasised filename, `size · uploader · date` meta line) and add ATT-04 inline image preview (64×64 thumbnail on the card, full-size preview in the existing focus-trapped Dialog, images served via `<img src>` only). Confirm the inline-image download branch (png/jpg/svg) from 02-01 with a dedicated test.

Purpose: Deliver the "recognise and preview" user capability. After this slice a user can identify each attachment at a glance and open an image preview without downloading.

Output: Full `AttachmentCard.tsx` per UI-SPEC, a new `ImagePreviewDialog.tsx`, human-size/date formatting helpers in `client.ts`, and the ATT-04 inline-image disposition test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-attachments-text-extraction/02-UI-SPEC.md
@.planning/phases/02-attachments-text-extraction/02-PATTERNS.md

# Prior slice contracts (this slice extends its UI + handlers):
@.planning/phases/02-attachments-text-extraction/02-01-SUMMARY.md

# Reuse these existing primitives verbatim:
@web/src/components/Dialog.tsx
@web/src/components/TrashView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full attachment card (thumbnail/type icon, filename, meta line) + formatting helpers</name>
  <files>web/src/api/client.ts, web/src/components/attachments/AttachmentCard.tsx, web/src/components/attachments/AttachmentCard.css, web/src/components/attachments/AttachmentsSection.tsx</files>
  <action>Add two pure helpers to `web/src/api/client.ts` (or a sibling util consistent with existing structure): `humanFileSize(bytes: number): string` (e.g. "1.4 MB", binary or decimal — pick one and document it; UI-SPEC shows "1.4 MB") and `humanDate(rfc3339: string): string` (e.g. "21 Jun 2026" via `toLocaleDateString` — NEVER a raw timestamp or SHA). Expand `AttachmentCard.tsx` to the UI-SPEC layout (ATT-03): a horizontal card (`--color-bg` bg, 1px `--color-border`, `--radius-md`, `--shadow-card`, `--space-md` padding) with a left 64×64 media square (`--radius-sm`): for image types (png/jpg/svg) an `<img src={downloadAttachmentUrl(id)}>` thumbnail with `object-fit: cover`; for others a `--color-surface` square with a centered lucide type icon (`FileText` for pdf/docx/txt, generic file icon otherwise, `--color-text-muted`). Filename: original_name at Body size, weight 600, ellipsis-truncated with full name in `title`. Meta line: `{humanFileSize(size_bytes)} · {uploader_name} · {humanDate(uploaded_at)}` at Label size, `--color-text-muted`, `·` separators. Download affordance (`.btn-ghost` or text link, accent) hitting `downloadAttachmentUrl(id)`. Co-locate `AttachmentCard.css` (token-only `var(--…)`; reference `TrashView.css` for card/row styles). Render cards from the `["attachments", pagePath]` query in `AttachmentsSection.tsx`. The opaque on-disk id is NEVER shown; no Git vocabulary anywhere (hidden-Git).</action>
  <verify>
    <automated>cd web && npm run build && npx tsc --noEmit && grep -RniE "\b(commit|sha|git|repo|push)\b" src/components/attachments/AttachmentCard.tsx | grep -v "import\|//" | grep -vq . && echo NO-GIT-VOCAB</automated>
  </verify>
  <done>Each attachment renders as the full UI-SPEC card: thumbnail-or-icon, emphasised filename, human-readable `size · uploader · date`; size/date are human-friendly; no opaque id or Git vocabulary appears; web build + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Inline image preview dialog + ATT-04 inline-disposition test</name>
  <files>web/src/components/attachments/ImagePreviewDialog.tsx, web/src/components/attachments/AttachmentCard.tsx, web/src/components/attachments/AttachmentCard.css, internal/server/handlers_attachments.go, internal/server/handlers_attachments_test.go</files>
  <behavior>
    - TestInlineImageDisposition (ATT-04): downloading a png/jpg/svg attachment returns `Content-Disposition: inline`, the real image Content-Type, AND `X-Content-Type-Options: nosniff`; an svg is still served as a downloadable image resource (consumed via <img>), never rendered as an HTML document.
  </behavior>
  <action>Create `ImagePreviewDialog.tsx` reusing the existing `Dialog` (focus trap + Esc/backdrop-cancel, backdrop never mutates): the dialog title is the original filename; the body is a single `<img src={downloadAttachmentUrl(id)}>` constrained to `max-width: 100%` and a viewport-bounded `max-height`, centered on `--color-bg`. SVG is shown via `<img src>` ONLY — never `dangerouslySetInnerHTML`, never inlined markup (stored-XSS guard from `MarkdownProse`, Pitfall 3). Wire `AttachmentCard.tsx`: clicking the image thumbnail (image types only) opens `ImagePreviewDialog`; non-image cards have no preview. Add the open/close state locally (mirror `DeleteConfirmDialog` open-state pattern). In `handlers_attachments_test.go` add `TestInlineImageDisposition` asserting the inline branch for png/jpg/svg (the disposition logic already exists from 02-01; this test pins ATT-04 explicitly and guards the svg-as-image contract). Confirm `isInlineImage` in the handler covers exactly image/png, image/jpeg, image/svg+xml.</action>
  <verify>
    <automated>go test ./internal/server/ -run TestInlineImageDisposition -count=1 && (cd web && npm run build && npx tsc --noEmit)</automated>
  </verify>
  <done>Clicking an image thumbnail opens a focus-trapped full-size preview rendered via `<img src>`; ATT-04 inline-disposition test passes (png/jpg/svg → inline + correct Content-Type + nosniff); SVG cannot execute script; non-image attachments show no preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored image → browser preview | An uploaded SVG/image is rendered in the user's browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering/Elevation (stored XSS via SVG) | ImagePreviewDialog / AttachmentCard thumbnail | mitigate | SVG shown via `<img src>` only — never `dangerouslySetInnerHTML`/inlined markup; an `<img>`-loaded SVG cannot run script (Pitfall 3); `X-Content-Type-Options: nosniff` on the download response |
| T-02-08 | Information disclosure | Card rendering | accept | Card shows original filename + uploader (intended team-visible metadata); opaque on-disk id is never exposed; no PII beyond the uploader display name already shown elsewhere |
| T-02-SC | Tampering | dependency installs | accept | No new dependencies in this slice (react-dropzone/react-query/lucide-react already present; no Go module added) |
</threat_model>

<verification>
- `go test ./internal/server/ -run TestInlineImageDisposition -count=1` passes (ATT-04).
- `cd web && npm run build && npx tsc --noEmit` succeed.
- Card shows filename + `size · uploader · date` + thumbnail/icon (ATT-03); manual UAT: thumbnail click opens the preview dialog (ATT-04).
- SVG safety: grep confirms no `dangerouslySetInnerHTML` in `web/src/components/attachments/`: `grep -rn "dangerouslySetInnerHTML" web/src/components/attachments/` returns nothing.
- Hidden-Git: no Git/SHA vocabulary in the rendered card copy.
</verification>

<success_criteria>
- ATT-03: every attachment renders as a full card (thumbnail/type-icon, emphasised filename, human `size · uploader · date`).
- ATT-04: image attachments (png/jpg/svg) preview inline via a focus-trapped Dialog using `<img src>` only; download response for images is inline + nosniff.
- No opaque id, raw timestamp, or Git vocabulary surfaces; web build + typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/02-attachments-text-extraction/02-02-SUMMARY.md` when done.
</output>
