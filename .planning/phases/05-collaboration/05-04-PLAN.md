---
phase: 05-collaboration
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02"]
files_modified:
  - internal/pages/forceedit_test.go
  - internal/pages/saveascopy_test.go
  - web/src/components/DiffReviewDialog.tsx
  - web/src/components/DiffReviewDialog.css
  - web/src/components/DiffReviewDialog.test.tsx
  - web/src/routes/PageEditor.tsx
  - web/src/routes/PageEditor.conflict.test.tsx
autonomous: true
requirements: [COLL-03, COLL-04]
must_haves:
  truths:
    - "A stale save returns 409 EVEN AFTER a force-edit took the lock — force-edit is lock-only and never bypasses the per-document revision check (the load-bearing rule)"
    - "On a 409 (explicit save OR autosave) the editor fetches the current server version and opens DiffReviewDialog in conflict mode with a real diff (old = their server version, new = my version) — never prose, never a silent overwrite"
    - "The conflict footer offers three choices ranked by risk: Overwrite (destructive, never default-focused), Manual merge (safe), Save as copy (safe, zero-loss); initial focus lands on a SAFE option"
    - "Save as copy creates a new page via Create/uniquePath and writes my body to it — the original page is never modified and never carries the conflicted base revision"
    - "Overwrite saves my version against the CURRENT revision; if another commit landed mid-overwrite it 409s again and re-opens the dialog (never a silent clobber)"
    - "While a conflict dialog is open, autosave stops re-arming so the dialog does not re-open every debounce"
  artifacts:
    - path: "internal/pages/forceedit_test.go"
      provides: "TestForceEditStillRejectsStaleSave — the COLL-03 load-bearing rule"
      contains: "func TestForceEditStillRejectsStaleSave"
    - path: "internal/pages/saveascopy_test.go"
      provides: "TestSaveAsCopyLeavesOriginal — copy is a fresh deduped page; original byte-identical"
      contains: "func TestSaveAsCopyLeavesOriginal"
    - path: "web/src/components/DiffReviewDialog.tsx"
      provides: "conflict mode (mode?: 'review' | 'conflict') with a 3-button footer; review mode unchanged; safe-focus inversion preserved"
      contains: "conflict"
    - path: "web/src/routes/PageEditor.tsx"
      provides: "409→dialog (supersedes the banner); Overwrite/Manual-merge/Save-as-copy handlers; autosave gated on !conflict"
      contains: "Save as copy"
    - path: "web/src/components/DiffReviewDialog.test.tsx"
      provides: "conflict-mode assertions: 3 buttons, focus NOT on Overwrite, real diff always rendered"
      contains: "conflict"
    - path: "web/src/routes/PageEditor.conflict.test.tsx"
      provides: "409 opens the dialog; autosave 409 does not drop the edit nor thrash"
      contains: "conflict"
  key_links:
    - from: "web/src/routes/PageEditor.tsx"
      to: "web/src/api/client.ts"
      via: "Overwrite = getPage(fresh rev)→savePage at fresh rev; Save-as-copy = createPage→getPage→savePage(newPath); 409 fetch = getPage"
      pattern: "getPage|createPage|savePage"
    - from: "web/src/routes/PageEditor.tsx"
      to: "web/src/components/DiffReviewDialog.tsx"
      via: "conflict state opens DiffReviewDialog mode='conflict' with oldText=server, newText=mine"
      pattern: "mode=.?conflict"
  prohibitions:
    - "No silent overwrite ever — every conflict choice routes through pages.Save's unconditional revision check; an Overwrite that races another commit 409s again and re-opens the dialog."
    - "Force-edit never bypasses the revision check — TestForceEditStillRejectsStaleSave is a first-class deliverable, not optional."
    - "Save as copy NEVER writes the original path and NEVER carries the conflicted base revision — it Creates a new path and Saves the new path at its fresh revision."
    - "Initial dialog focus is NEVER on Overwrite (the data-losing action) — a reflexive Enter must not discard the server's change. Keep the Phase 4 focus-inversion; document it so a refactor cannot 'fix' it."
    - "No Git vocabulary in any conflict string ('merge conflict'/'commit'/'HEAD'/'branch'/'SHA' forbidden) — use the verbatim UI-SPEC copy."
    - "pages.Service.Save is NOT modified — the 409 floor already exists; this slice only proves and consumes it."
---

<objective>
Slice 4 of Phase 5 (own plan, the load-bearing slice): deliver COLL-04 (conflict resolution: overwrite / manual-merge / save-as-copy) AND harden+prove COLL-03 (force-edit never bypasses optimistic concurrency).

This slice depends on nothing from the presence/lock UI beyond Slice 1's force-edit being lock-only — but it is where the safety contract is *verified*. The headline deliverable is `TestForceEditStillRejectsStaleSave`: a forced lock + a landed commit + a save with the old base_revision must still return `ErrStaleRevision` (409). pages.Service.Save is NOT changed — its unconditional revision check (service.go:200) already is the sole data-loss authority; force-edit (Slice 1) is lock-only, so a stale forced save still 409s by construction. The frontend extends the Phase 4 `DiffReviewDialog` with a `conflict` mode (3-button footer, safe-focus) and wires the three handlers; every choice routes through the EXISTING save path — no new conflict endpoint.

Purpose: when two people collide, the loser of the race sees a real diff and three safe-by-ranking choices — and can never silently lose data, including past a forced lock.
Output: the two COLL-03/04 backend tests, the DiffReviewDialog conflict mode + its tests, the PageEditor conflict handlers + autosave gating + its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-collaboration/05-RESEARCH.md
@.planning/phases/05-collaboration/05-PATTERNS.md
@.planning/phases/05-collaboration/05-UI-SPEC.md
@.planning/phases/05-collaboration/05-VALIDATION.md
@.planning/phases/05-collaboration/05-01-SUMMARY.md
@.planning/phases/05-collaboration/05-02-SUMMARY.md

# Analogs to copy (read before writing)
@internal/pages/service.go
@internal/pages/service_test.go
@internal/pages/commitjob_test.go
@internal/locks/service.go
@web/src/components/DiffReviewDialog.tsx
@web/src/routes/PageEditor.tsx
@web/src/api/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: COLL-03 load-bearing test — force-edit never bypasses the stale-revision check</name>
  <files>internal/pages/forceedit_test.go</files>
  <behavior>
    - Acquire a soft lock on a page as session A; force-take it as session B (locks.Force — lock-only).
    - Land a real commit on that page through the single-writer save path (a successful Save advances the committed revision).
    - Have B save with the base_revision it captured BEFORE the landed commit → Save returns ErrStaleRevision (the 409 floor fires regardless of who holds the lock).
    - Control: B saves with the CURRENT revision → succeeds (force-edit is not blocked; only a STALE save is rejected).
  </behavior>
  <read_first>
    - internal/pages/service.go:176-220 (Revision + Save: the check at :200 `if current != baseRevision { return ErrStaleRevision }` is BEFORE any write; ErrStaleRevision at :29).
    - internal/pages/service_test.go:60-130 (newServiceFixture / newIndexedFixture: a real repo+git+worker with the CommitJob registered and the drain running; injected `now`; how a test reads Revision and asserts on-disk/committed state).
    - internal/pages/commitjob_test.go:15-44 (newTestRepoAndGit + the git skip-guard — needed here because Save commits).
    - internal/locks/service.go (locks.Service.Acquire/Force from Slice 1 — used to set up the forced-lock precondition; assert Force touched only the lock, never the page).
  </read_first>
  <action>
    Create `internal/pages/forceedit_test.go` (package pages). Build a pages.Service via the existing fixture (newServiceFixture / newIndexedFixture) and a locks.Service over the SAME repo. Write `TestForceEditStillRejectsStaleSave`:
    1. Create a page; read rev0 = Service.Revision(page).
    2. locks: A Acquire(page, ownerA); B Force(page, ownerB) — assert Force returns B and did NOT change the page's committed revision (Revision(page) == rev0; the page body on disk is unchanged — Force is lock-only).
    3. Land a commit: Save(page, body="v2", baseRevision=rev0, userX) succeeds (this is the "someone else saved" event); read rev1 = Revision(page); assert rev1 != rev0.
    4. The forced editor B saves stale: Save(page, body="B's edit", baseRevision=rev0 /* the rev B opened at, now stale */, userB) → assert errors.Is(err, ErrStaleRevision). Assert the on-disk body is still "v2" (B's stale write never landed).
    5. Control: Save(page, body="B's edit", baseRevision=rev1, userB) → no error; the write lands.

    This proves force-edit (a lock op) never bypasses Save's revision check — by construction, since Save reads the committed revision itself and nothing in the save path is lock-aware. Do NOT modify pages/service.go.
  </action>
  <verify>
    <automated>go test ./internal/pages/ -run TestForceEditStillRejectsStaleSave -count=1</automated>
  </verify>
  <done>TestForceEditStillRejectsStaleSave passes: a forced lock plus a landed commit still makes a stale save 409 (ErrStaleRevision), the stale body never lands, and a save at the current revision succeeds; pages/service.go is untouched; the test also asserts Force changed only the lock, not the page revision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: COLL-04 backend test — save-as-copy creates a fresh deduped page and leaves the original intact</name>
  <files>internal/pages/saveascopy_test.go</files>
  <behavior>
    - Create an original page with known body + a known revision.
    - Perform the save-as-copy sequence: Create(folder, "{Title} (Copy)") → a NEW deduped path with a FRESH revision → Save(newPath, my body, freshRev).
    - The original page's body and committed revision are byte-identical to before (never modified, never carries the conflicted base revision).
    - The copy exists at the deduped path with my body; a second copy auto-dedups (uniquePath appends -2).
  </behavior>
  <read_first>
    - internal/pages/service.go:115-143 (Create — scaffolds a BLANK doc, takes NO body, returns a new path; so save-as-copy is two steps: Create then Save), :318 (uniquePath — appends -2/-3 until free), :176-220 (Revision + Save).
    - internal/pages/service_test.go:60-130 (fixtures; how to read a page body + revision off disk/committed state).
  </read_first>
  <action>
    Create `internal/pages/saveascopy_test.go` (package pages). Write `TestSaveAsCopyLeavesOriginal`:
    1. Create original page "notes/plan.md" with title "Plan"; Save it body="ORIGINAL"; record origRev = Revision + origBody on disk.
    2. Save-as-copy sequence (the backend half of COLL-04): newPath = Create(folder="notes", title="Plan (Copy)") → assert newPath != original and is a deduped sibling; freshRev = Revision(newPath); Save(newPath, body="MINE", baseRevision=freshRev) → no error.
    3. Assert: Revision("notes/plan.md") == origRev AND its body is still "ORIGINAL" (original untouched, never carries a conflicted base); the copy's body == "MINE".
    4. Dedup: a SECOND Create(folder="notes", title="Plan (Copy)") returns a different path (e.g. "-2") — uniquePath never collides.

    This is the deterministic proof of CONTEXT's "fresh revision, original untouched." The frontend save-as-copy handler (Task 4) drives exactly this sequence via createPage/getPage/savePage.
  </action>
  <verify>
    <automated>go test ./internal/pages/ -run TestSaveAsCopyLeavesOriginal -count=1</automated>
  </verify>
  <done>TestSaveAsCopyLeavesOriginal passes: the copy is a fresh deduped page carrying my body and a fresh revision; the original's body and revision are unchanged; a repeat copy auto-dedups.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DiffReviewDialog conflict mode (3-button footer, safe-focus) + tests</name>
  <files>web/src/components/DiffReviewDialog.tsx, web/src/components/DiffReviewDialog.css, web/src/components/DiffReviewDialog.test.tsx</files>
  <behavior>
    - In conflict mode the footer renders exactly three buttons: Overwrite (destructive), Manual merge (secondary), Save as copy (secondary). Review mode (Approve/Reject) is unchanged.
    - On open, initial focus is on a SAFE control (Save as copy or Manual merge) or the diff region — NEVER Overwrite.
    - The real diff is ALWAYS rendered (old = their server version, new = my version); if oldText === newText, show the "identical" message + a single safe Save (never a fabricated diff).
    - Esc / backdrop = cancel (apply nothing). Overwrite/Save-as-copy show a busy state on click.
  </behavior>
  <read_first>
    - web/src/components/DiffReviewDialog.tsx:7-19 (the doc comment: built to be REUSED in Phase 5; the deliberate focus-inversion — focus the SAFE button, "do NOT fix it by focusing Approve"), :25-51 (props; oldText/newText/onApprove/onReject/stale/busy), :56-79 (rejectRef + the focus-on-open effect; preserve this for the conflict safe-focus), :68-73 (splitView + noChange === oldText===newText guard), :82-100 (Esc/backdrop cancel + focus-trap), :178-248 (the review footer — add the conflict branch alongside, leaving review untouched).
    - .planning/phases/05-collaboration/05-UI-SPEC.md §DiffReviewDialog Conflict Mode Trust Contract (the load-bearing rules: ranking, safe-focus, identical-versions, busy, color-not-sole-signal) + §Copywriting Contract (verbatim strings) + §Color (Overwrite is the ONLY destructive control).
  </read_first>
  <action>
    Extend DiffReviewDialog with `mode?: "review" | "conflict"` (default "review"). The review path is UNCHANGED. Add a discriminated conflict prop set: `onOverwrite`, `onManualMerge`, `onSaveAsCopy`, plus the busy variants (e.g. busy?: "overwrite" | "copy"). In conflict mode:
    - Title: "This page was changed somewhere else" (an AlertTriangle in `--color-destructive` may precede it). Summary: "Someone saved a different version while you were editing. Compare your version with theirs, then choose what to keep." Column caption: "Left: the saved version · Right: your unsaved version". (oldText = their server version on the left; newText = my version on the right.) All copy VERBATIM from the UI-SPEC; NO Git vocabulary.
    - Footer = three buttons ranked by risk: Overwrite (`.btn-ghost-destructive` or `.btn-destructive` — the ONLY destructive control; positionally isolated from the safe pair, with the risk sub-line "This replaces the other person's changes."), Manual merge (`.btn-secondary`), Save as copy (`.btn-secondary`). DOM-order the safe button(s) first so the focus trap lands there.
    - Focus: point the existing focus ref (rejectRef analog) at Save as copy (or Manual merge) — NEVER Overwrite. Reuse the Phase 4 focus-on-open effect verbatim. Keep the "do NOT focus the destructive action" comment so a future refactor can't regress it.
    - noChange (oldText === newText): render "These versions are identical — your save will go through." + a single safe "Save" button (never fabricate a diff).
    - Inherit untouched: focus-trap/Tab-wrap, Esc/backdrop = cancel-only, real-diff-always-rendered, splitView toggle, mono diff theme, busy state.

    DiffReviewDialog.css: add only what the 3-button conflict footer needs (the destructive button + the wrap that isolates Overwrite from the safe pair + the risk sub-line) — token-only (`var(--color-destructive)`, `--space-sm`); at narrow widths the three buttons wrap with Overwrite isolated. No hard-coded hex/px.

    Extend DiffReviewDialog.test.tsx (do not fork) with conflict-mode assertions: (a) exactly three footer buttons render in conflict mode with the verbatim accessible names; (b) on open, document.activeElement is NOT the Overwrite button (it is Save as copy / Manual merge or the diff region); (c) the diff is rendered (the react-diff-viewer region is present) for differing oldText/newText; (d) oldText===newText shows the identical-versions message and a single Save; (e) Esc calls the cancel handler and none of overwrite/merge/copy. Keep the existing review-mode tests green.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/components/DiffReviewDialog.test.tsx</automated>
  </verify>
  <done>DiffReviewDialog has a conflict mode with a 3-button risk-ranked footer, safe initial focus (never Overwrite), always-real-diff, the identical-versions guard, and inherited Esc/backdrop-cancel + focus-trap; review mode is unchanged; the extended test file is green including the focus-not-on-Overwrite assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: PageEditor — 409→dialog, the three conflict handlers, autosave gating + test</name>
  <files>web/src/routes/PageEditor.tsx, web/src/routes/PageEditor.conflict.test.tsx</files>
  <behavior>
    - A 409 from an explicit Save OR from autosave fetches the current server version and opens DiffReviewDialog (mode="conflict", oldText=server body, newText=my body) instead of the old banner.
    - While the dialog is open, autosave stops re-arming (no re-open every debounce); the in-flight edit is never dropped.
    - Overwrite → getPage(fresh rev) → savePage at fresh rev; a re-409 re-opens the dialog with the newer server version.
    - Manual merge → close the dialog, keep my body, surface the server version for reference, then a normal (revision-checked) save.
    - Save as copy → createPage(folder, "{title} (Copy)") → getPage(newPath) for fresh rev → savePage(newPath, mine) → navigate to the new page; the original is never written.
  </behavior>
  <read_first>
    - web/src/routes/PageEditor.tsx:47-52 (conflict state + baseRevision ref), :92-150 (runSaver — the single-flight saver; the 409 catch at :116-128 sets conflict and returns with content intact and does NOT advance baseRevision), :167-170 (scheduleAutosave — gate on !conflict here), :209-212 (explicit Save), :218-237 (the existing 409 banner slot — supersede with the dialog), :248 (readField(frontmatter, "title") — the copy title source), :139-147 (how getPage's fresh revision is read after a save).
    - web/src/api/client.ts:222-253 (getPage returns {frontmatter, body, revision}; createPage(folder,title)→{path}; savePage(path,{body,frontmatter,base_revision})).
    - .planning/phases/05-collaboration/05-UI-SPEC.md §Conflict Mode Trust Contract (Overwrite re-409 → re-open; Save-as-copy navigate; busy copy) + §Copywriting (KEEP/SUPERSEDE the Phase 1 banner).
  </read_first>
  <action>
    Repurpose the existing `conflict` state to drive the dialog. On a 409 (both the explicit-Save path and the autosave path already funnel through runSaver's catch at :116-128), fetch the server version via getPage(path) and open DiffReviewDialog with mode="conflict", oldText = server.body, newText = my current editor body. Keep server.revision around for Overwrite. Supersede the :218-237 banner for the save-collision path (keep its calm voice if any fragment remains, but the dialog is the resolution surface).

    Gate autosave: scheduleAutosave (:167-170) must NOT re-arm while `conflict` is open (Pitfall 5) — otherwise the dialog re-opens every debounce. Resolve → clear conflict, advance baseRevision, resume autosave.

    Three handlers wired to the dialog:
    - onOverwrite: getPage(path) → fresh = {body?, revision}; savePage(path, { body: mine, frontmatter: mine, base_revision: fresh.revision }). On success: clear conflict, set baseRevision = the new revision, show transient "Saved". On a re-409 (another commit landed between the fetch and the save): re-open the dialog with the NEWER server version (never a silent clobber). Show the inherited busy state ("Saving…") while in flight.
    - onManualMerge: close the dialog, keep my body in the editor, surface the server version for reference (exact layout is discretion — a read-only reference pane / collapsible; the server version MUST stay visible while reconciling), then let a normal Save run (which itself revision-checks). No backend call beyond the eventual Save.
    - onSaveAsCopy: title = readField(frontmatter, "title"); folder = dirname(path); newPath = createPage(folder, `${title} (Copy)`); fresh = getPage(newPath); savePage(newPath, { body: mine, frontmatter: mine-or-copy-title, base_revision: fresh.revision }); then navigate to newPath (transient "Saved as a copy. Opening it now."). NEVER write the original path; NEVER reuse the conflicted base revision. (Backend proof: Task 2.)

    Esc/backdrop on the dialog = cancel: leave my edits in the editor, the server untouched, conflict resolvable on the next save (inherited).

    Create `web/src/routes/PageEditor.conflict.test.tsx`: mock the client (savePage rejects 409 once; getPage returns a server version) and assert: (a) a 409 opens the conflict dialog with a real diff and the in-flight body intact (not dropped); (b) while the dialog is open, advancing the debounce timer does NOT fire another save / re-open (autosave gated); (c) Save-as-copy calls createPage then savePage(newPath, …) and navigates, and never calls savePage(originalPath); (d) Overwrite calls getPage then savePage(originalPath, base_revision = fresh rev). Use the existing test utilities/mocks pattern in the repo.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/routes/PageEditor.conflict.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>A 409 (explicit or autosave) opens the conflict dialog with a real diff and the edit intact; autosave is gated while the dialog is open; Overwrite saves at the fresh rev and re-409s safely; Save-as-copy creates+saves a new page and navigates without touching the original; Manual merge keeps my body with the server version visible; the conflict test is green and tsc is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent save → committed page | Two writers race; the server-side revision check is the only authority that prevents silent overwrite. |
| forced lock → save path | A forced lock must NOT grant a write that skips the revision check. |
| conflict choice → page bytes | Overwrite is the one data-losing choice; it must be reviewed, ranked, and re-checked; Save-as-copy must never touch the original. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan | Req |
|-----------|----------|-----------|-------------|-----------------|-----|
| T-05-15 | Tampering | pages.Save revision check (post force-edit) | mitigate | The unconditional `current != baseRevision → ErrStaleRevision` check (service.go:200) is the sole data-loss authority and is NEVER bypassed, including after a forced lock (force is lock-only). Proven by TestForceEditStillRejectsStaleSave. This is THE load-bearing rule. | COLL-03 |
| T-05-16 | Tampering | Overwrite race | mitigate | Overwrite fetches the current revision then Saves at it; if another commit lands in between, Save 409s again and the dialog re-opens with the newer server version — never a silent clobber. | COLL-04 |
| T-05-17 | Tampering | Save-as-copy | mitigate | Create a NEW deduped path (uniquePath) then Save the new path at its FRESH revision; the original path is never written and never carries the conflicted base. Proven by TestSaveAsCopyLeavesOriginal. | COLL-04 |
| T-05-18 | Tampering (UX→data) | dialog default focus | mitigate | Initial focus is NEVER on Overwrite (the data-losing action); a reflexive Enter cannot discard the server's change. Asserted in DiffReviewDialog.test.tsx; comment guards against a refactor regression. | COLL-04 |
| T-05-19 | Tampering (XSS) | diff/old/new + copy title | mitigate | Diff content + names render via React/react-diff-viewer (escaped), never dangerouslySetInnerHTML; the copy title is plain text fed to createPage which path-validates server-side. | COLL-04 |
| T-05-SC | Tampering | npm/go installs | mitigate | No new dependencies this slice (react-diff-viewer-continued already wired in Phase 4) — nothing to vet. |  |
</threat_model>

<verification>
- `go test ./internal/pages/ -run 'TestForceEditStillRejectsStaleSave|TestSaveAsCopyLeavesOriginal' -count=1` green.
- `cd web && npx vitest run src/components/DiffReviewDialog.test.tsx src/routes/PageEditor.conflict.test.tsx` green.
- `cd web && npx tsc --noEmit` clean.
- `git diff --name-only internal/pages/service.go` shows NO change to service.go (the 409 floor is reused, not modified).
- `grep -niE 'merge conflict|commit|HEAD|branch|\bSHA\b|repo|git' web/src/components/DiffReviewDialog.tsx` returns no user-facing Git vocabulary in conflict copy.
- Manual (UAT, VALIDATION.md): two sessions race a save; the loser sees the dialog with a real diff; Overwrite replaces, Save-as-copy creates a copy and leaves the original intact, Manual merge keeps my body with theirs visible.
</verification>

<success_criteria>
COLL-04 is delivered (real-diff conflict dialog with three risk-ranked, safe-by-default choices, all routed through the existing save path) and COLL-03 is hardened+proven (force-edit never bypasses the per-document revision check; a stale forced save still 409s). No silent overwrite is possible by any path; save-as-copy never mutates the original; pages.Service.Save is unchanged.
</success_criteria>

<artifacts_produced>
- `internal/pages/forceedit_test.go` — TestForceEditStillRejectsStaleSave (COLL-03 load-bearing).
- `internal/pages/saveascopy_test.go` — TestSaveAsCopyLeavesOriginal (COLL-04 backend).
- `web/src/components/DiffReviewDialog.tsx` + `.css` — conflict mode (modified).
- `web/src/components/DiffReviewDialog.test.tsx` — conflict-mode assertions (extended).
- `web/src/routes/PageEditor.tsx` — 409→dialog + 3 handlers + autosave gating (modified).
- `web/src/routes/PageEditor.conflict.test.tsx` — conflict-flow component test (new).
</artifacts_produced>

<output>
Create `.planning/phases/05-collaboration/05-04-SUMMARY.md` when done.
</output>
